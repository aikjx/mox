name: flow-bridge
version: 0.1.0
description: "注入 flow-ai + expert-alliance 流程图/关系网优化内核（璇玑验证网关）"
author: algorithm-alliance
kind: plugin
dependencies:
  - expert-alliance-server   # 独立常驻优化服务（POST /api/optimize, POST /api/verify）
