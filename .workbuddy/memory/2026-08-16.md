# 2026-08-16

## 璇玑全维需求业务处理流程图（归一化企业级）
- 整理 infotopograph 项目内"璇玑/璇玑"（`crates/mox-expert` + `mox-system`）处理流程，产出归一化、企业级交付物。
- 核心事实来源：`mox-expert/DESIGN.md`、`src/pipeline.rs`、`src/verify.rs`(⛨璇玑5项阻断检查)、`src/govern.rs`(治理闸门)、`GR-STD-信息关联关系图开发规范-V1.0.md`、`关图骨架定义.md`、`guantu.req.json`。
- 模型落点：归一化 IR + 双璇玑十四维（业务七维+开发七维）+ 关图六维绑定 REQ→FUN→BIZ→ALG→TSK→COD + 4 道闸门（G0 归一化 / G1 裁决 / G2 ⛨璇玑否决最高权限 / G3 治理）。
- 产出文件：`docs/璇玑-全维需求业务处理流程图-归一化企业级.html`（可视）、`docs/璇玑-全维需求业务处理流程图-归一化企业级.md`（纳入版本管理）。
- 续作（"继续"）：① 展开最高权限闸门 `docs/璇玑-璇玑验证子流程-归一化企业级.html`（S6 五检查 + expert_veto/escalated_conflict 并入）；② 产出可机读单一源 `docs/璇玑-全维流水线.mmd`（Mermaid，全链路 + S6 子图 + 六维绑定 + 外部能力归一化）。

## 续作：TraceMatrix 六维绑定追溯（AA-TRACE-V1.0）
- 方向：闭环 AA-STD-V1.0 §3「导出 TraceMatrix」，把"璇玑=D04"落到真实代码节点（对齐 `primiflow/src/generate.rs` 既有 `trace_matrix.md` 机制）。
- 真实落点：`guantu.req.json:6` D04=全维治理/璇玑；`:31` D04--Bind-->CodeFile:crates/mox-expert/src/lib.rs。函数入口：normalize_requirement(programming.rs:108)、programming_pipeline(:159)、mox_optimize(pipeline.rs:41)、auto_dimension(ir.rs:185)、run_experts(harness.rs:361)、reconcile(:98)、verify(verify.rs:43)、govern(govern.rs:126)、verify_chain(audit/integration.rs:116)。
- 关键归一发现：编码层 ①-⑩ 中 ⑦ emit 在 ⑨ 治理闸门之前，AA-STD 为"闸门先于出码"。已收口为 AA-STD 时序为唯一基准（出码仅生成草稿，须经治理闸门 approved 方可交付，与 G-C 三证一致）。两套编号分歧不再并存。
- 产出：`璇玑-全维分析-TraceMatrix-六维绑定追溯.md`（含双编号映射+Six-Dim+TraceMatrix主表+关系图）、`mox-tracematrix.html`（自包含可视，浅色企业级）。

## 开发 + 测试验证（mox-expert 全维分析需求）
- 用户指令升级为"开发好所有架构功能并测试分析验证"。结论：**架构功能代码层已全落地**（比早前文档更快），142 项测试全绿（86 单元 + 56 集成），无 todo!/unimplemented! 假实现。
- 实际缺口极小：仅 `mox_optimize` 的 `HarnessProfile.plugins` 硬编码 7 业务专家、与 `all_experts()` 双璇玑十四维漂移。已修正为 `plugins: experts.iter().map(|e| e.id()).collect()`（pipeline.rs:42-56），重测零回归。
- 产出：`璇玑-全维分析需求-测试分析验证报告.md`（架构功能→测试证据映射表 + 归一化修正 + 风险）。
- 残留漂移（建议）：三份文档用三套编号（①-⑩+G-A~G-E / S1-S8+G0-G3 / C1-C8+六维）；建议归一到 AA-STD-V1.0 时序基准。代码层已统一，不影响运行时。
