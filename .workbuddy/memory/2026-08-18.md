# 2026-08-18 工作日志

## docs/ 全量归一化（企业级）

- 完成 `docs/` 65 份文档的审计 + 物理归一化 + 引用修复 + 治理文档更新。
- 关键动作：
  - 7 份 `*-20260816` 过程稿 → `docs/_archive/2026-08-16/`（git mv 保留历史）。
  - `guantu-skeleton.md`、`xuanji-requirement-baseline.md` → `docs/full-dimensional/`（与索引声明对齐，修复 F2）。
  - 10 份松散模块文档（market/automation/mathematical/business-flow(s/charts)/xuanji-expert-*/PrimiFlow-设计蓝图）→ `docs/modules/`。
  - ISD 验收报告 `璇玑-信息化系统开发验收报告-V1.0.{md,html}` → `docs/enterprise/`，并在 `00-INDEX` §1.2 注册为 🟢 权威交付物。
  - viz 同位化：`xuanji-tracematrix.html`→full-dimensional、`xuanji-flow.html`→modules。
  - 24 文件 60+ 处交叉引用按 `docs/<rel>` 仓根相对形式全量重写（脚本幂等）。
- 产出治理活文档：`docs/DOC-NORMALIZATION-REPORT.md`（DOC-GOV-V1.0，含审计发现 F1-F11、标准、逐文件动作、残留 GAP）。
- 残留 GAP：GAP-1 `enterprise-architecture-analysis.md:216` 引用 `algorithm-verification.md` 但该文件不存在（待建或删）。
- 验证：旧根路径引用扫描 0 命中；索引声明=物理事实一致。

## backend/ 与 crates/ 双后端冲突（架构体检）

- **发现**：仓库存在两套完整后端实现同一关图/璇玑系统，均监听 `:3000`、均托管 `frontend/dist`、前端 `baseURL='/api'` + Vite 代理 `:3000`。
  - `crates/`（Rust/Axum，CLAUDE.md 指定为正式后端）：含 ⛨验证网关 + 治理8闸门 + 多方言 repo，出码必经 `xuanji-expert::verify` 与 `primiflow-fusion::full_gate`。
  - `backend/`（ous-backend，零依赖 Node，working memory 记为当前生产入口）：**纯 JS 重写**了双璇玑十四维治理（`xuanji.runAlliance`/`governGate` 8闸门）、知识图谱（`graph.js`）、算子商城、MCP JSON-RPC、NL 编译器、自动化中枢，且自带 `/api/xuanji/publish` —— **完全不调用 Rust，绕过 ⛨网关/8闸门**。
- **判定**：既是重复建设（同域两套实现），也是合规风险（Node 出码不经 Rust 强制闸门）。
- **推荐方向（待用户确认才执行）**：以 `crates/` 为唯一系统真相；`backend/` 降级为纯静态托管 + `/api` 反向代理到 Rust（消除重复与合规漏洞），保留其零依赖一键起前端的便利。此即「修复→优化→融合」。
- **已执行（2026-08-18，用户指令「Node 修改为 Rust」）**：`backend/` 改为纯边缘入口（静态托管 `frontend/dist` + `/api` 反代到 Rust），删除 Node 重复逻辑（routes/xuanji/graph/store/seed 及 3 个测试）；`runtime` 默认端口 `3000→3001`，Node 占 `:3000` 作系统入口；前端 Vite 代理 `/api→:3000` 不变；CLAUDE.md 部署说明已同步。残留缺口：Rust 未实现 `/api/automation/chat`、`/api/market/random`、`/api/market/upload`（Node 专有轻量端点），需后续在 Rust 补或前端改调。

## docs/ 第二轮：修复+整合+死链归零（企业级）

- 精确死链审计（仅判定指向 `.md/.html` 文档产物的引用，排除代码/通配符噪声）：修复前发现 5 类真实错误。
- **修复内容**：
  - 文件名笔误 `xuanji-expert-xuanji-fusion-flows.md` → `xuanji-expert-alliance-fusion-flows.md`：02-architecture.md:295、04-business-processing.md:197、xuanji-expert-business-requirements.md:5/234（含 `./` markdown 链接）、graph.mmd:88（节点标签）。
  - 相对路径补 `docs/` 前缀：`00-INDEX.md:83` 的 `full-dimensional/00-README.md` → `docs/full-dimensional/00-README.md`。
  - **GAP-1 收口**：新建 `docs/modules/algorithm-verification.md`（AV-STD-V1.0，🟢 权威）——以仓内真实校验源（`verify_axioms.py` 六公理+守恒律、`pt-primi-架构规范` §9 的 ε≤1e-3/六维绑定、`GOVERNANCE_CONSOLE_API` ⛨网关+AuditChain）定义 L1 数学自洽/L2 PT-Primi 合规/L3 璇玑治理闸门统一验证矩阵；同步修正 enterprise-architecture-analysis.md:216 引用并在 00-INDEX §1.2 注册（计数 17→18）。
- **整合评估**：段落级重复扫描发现 `guantu-skeleton.md`(🟢) 与 `关图骨架定义.md`(🟡过程稿) 共享 8 段（raw→normalized 衍生关系）；判定为设计内追溯稿，保留不强行合并（保单一事实源），仅记入报告 §7 明确处置。
- **回归**：重跑死链审计真实断链 **0 处**（残余 41 条全为通配符/源码/目录树代码块/报告描述性文字噪声）；git 显示 6 改 + 1 新增（algorithm-verification.md 未提交，待用户 commit）。
- 刷新 `docs/DOC-NORMALIZATION-REPORT.md`（DOC-GOV-V1.0）：F9→已解决、新增 F12/F13、FIX-10~14、§7 整合评估、验证结论更新。
