# 2026-08-18 工作日志

## 关键发现
- 此前会话声称「后端已完成并验证」，但 `backend/` 目录实际**不存在于磁盘**——只停留在叙述，未落盘。前端 `frontend/dist` 仅有 105 个陈旧资产（之前 vite build 因 EPERM 失败）。
- 根因：早期 `vite build` 的 EPERM 是因为**运行中的后端进程持有 `frontend/dist` 文件锁**（静态托管该目录），导致 Vite 写资产失败。并非权限/只读问题。

## 今日完成（算子统一系统 OUS 真·前后端贯通）
1. 从零实现**零依赖 Node.js 后端** `backend/`（仅用内置 `http`/`fs`/`path`，因本沙箱 `npm install` 完全不可用）：
   - `src/store.js`：内存 Map + JSON 落盘（`backend/data/*.json`）。
   - `src/graph.js`：真实算法——PageRank(0.85 阻尼)、度/中介中心性、标签传播社区发现、BFS 最短路、激活传播、共同邻居推荐、导入导出。
   - `src/xuanji.js`：**双璇玑十四维治理引擎**（业务7+开发7 → 归一化裁决 → ⛨璇玑验证网关(5检查) → 8 闸门 → 优化出图 → 融合发布），输出严格对齐 `XuanjiFusionView` 响应结构（score / gate / gate_detail.gates / algorithm_veto / optimized_graph）。
   - `src/seed.js`：23 图节点 + 29 边 + 算子/商城/插件/工作流/流程/资源/模板种子。
   - `src/routes.js`：覆盖前端 `frontend/src/api/index.js` 约定的**全部 ~80 个 /api 端点**（系统/算子/图谱/AI/市场/Caomei/MCP/自动化/璇玑）。
   - `src/server.js`：HTTP 路由（字面优先 + :param）、Bearer 鉴权(dev-secret-token)、请求日志、**静态托管 frontend/dist 作为系统统一入口**。
2. `vite build` 成功产出 `frontend/dist`（25s，XuanjiFusionView/GraphView 等全部视图入包）。
3. 端到端验证（curl 全绿）：health、graph(centrality/communities/pagerank/activate/path/recommend/neighbors/search)、xuanji/optimize(score=80,gate=G3-通过,8闸门全过)、xuanji/publish(task_done=true→published)、automation/run(passed)、mcp(tools/list+call)、caomei/compile、market/upload/clone、flows/validate 均正确。

## 工程约定（重要，留痕）
- **构建前端前必须先停后端**：后端静态托管 `frontend/dist`，持有文件锁；构建时若后端在跑必现 EPERM。流程：停后端(PID on :3000) → `rm -rf frontend/dist` → `node node_modules/vite/bin/vite.js build` → 重启后端。
- 后端用托管 Node `C:/Users/mo/.workbuddy/binaries/node/versions/22.22.2/node.exe`（系统默认 node 为 10.x，不可用）。
- 启动：`cd backend && node src/server.js`（默认 3000 端口），系统入口 http://localhost:3000/ 。
- 开发期前端若独立 `npm run dev`，其 Vite 代理 `/api → localhost:3000` 也需后端在跑。

## 企业级测试套件（本轮新增）
- 新增 `backend/tests/`：**零依赖**测试（Node 内置 assert + 自研 `_harness.js`），`npm test` 即 `node tests/run.js`。
  - `unit.test.js`：图算法（PageRank/度·中介中心性/社区发现/最短路/激活传播/邻居/推荐/normEdge）+ 双璇玑引擎（归一化环检测·孤立、14维派发、裁决冲突、验证网关 veto、治理闸门 G3-通过/驳回、runAlliance、融合发布双验收）。
  - `integration.test.js`：启动真实 `createServer` 实例（随机端口 + 临时数据目录），经 fetch 全量验证 60+ 端点（系统/图谱/MCP-JSON-RPC/市场/算子/自动化/AI/安全401/404/静态 SPA）。
  - 为可测性重构：`server.js` 抽出 `createServer()` 工厂（仅 `require.main===module` 时自动 listen）；`store.js` 支持 `OUS_DATA_DIR` 环境变量隔离测试数据。
  - 新增 SIGINT/SIGTERM 优雅关闭（落盘后退出，5s 兜底强退）。
- **测试结果：62 通过 / 0 失败**（全绿）。生产服务已重启并验证：`/api/health`=200、`GET /`=200(html)、`/xuanji/optimize`→G3-通过(79分,14维)。
- 运行测试：`cd backend && node tests/run.js`（或 `npm test`）。
