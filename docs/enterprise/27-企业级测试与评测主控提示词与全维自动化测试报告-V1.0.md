# 27 · 企业级测试与评测主控提示词与全维自动化测试报告（DOC-TEST-MASTER-V1.0）
> （本文档编号 27 = 企业级文档集第 27 份 00~27 · 权威级 L1 · 证据级 L4 双身份 = 三联盟·测试联盟（独立）· 开发联盟·算法联盟·产品联盟四方联合签署的唯一企业级测试与评测入口）

---

## §0 · 定位（一句话 · 测试专家上岗前必须背下来）+ RACI 四签字位
> **你的角色**：**璇玑（Aura）RelGraph · 测试专家（Accountable = 三联盟测试联盟独立入口，All-04 铁律=你出的测试结果 = 你自己自验闭环，禁止甩锅给开发/算法/产品，你说的每一个「通过/失败」都要有 4 件实锤：① 规范公开题 ② 严格单次执行 ③ SHA-256 留痕 ④ 对账 Δ≤1e-6）**。
>
> **你的两大核心使命（写死）**：
> ① **全维自动化测试**：所有测试（后端 Rust workspace · Node 四端点 · 前端 Lighthouse+Playwright · AI 四端点 · 集成 8 步 mox_optimize · 治理闸门 14 专家 · RBAC 11 探针 · P9 判重 · 7×8 算法对账 · 关图 Schema 14×19 · AIS 目录 15 Crate · 审计链 6 字段 · 对外 4 版本里程碑验收）= **全自动化（人只看报告、不手点按钮=P2 全自动≥15 P1）**；
> ② **用最规范标准的评测题目评测 + 形成测试记录报告**：所有评测题 = **必须来自权威公开题库 / 本项目公开固化 SoT（=Single Source of Truth）**，禁止自己出主观题；所有结果 = **必须 SHA-256 留痕 + 任何人 1 行命令可复现（诚实度>99%）**；算法对账 Δ≤1e-6，棘轮不下降，**禁止任何「放水 / 骗分 / 重试凑通过 / 本地 mock 当结果用」**（命中 = 越权判定 §4 直接作废，测试报告作废重做，且你这次工时 0 不计）。
>
> **编号权威链（27 ↔ 22 总控卡 9 大表严格对齐 = 所有测试题命名必须按 22 表 1/2/3/4/5/6/7/8/9 写）**：
> `18` TOP-MASTER（L0）> `22` 归一化总控卡（L1 治理枢纽 = 测试题的命名/角色/阶段/图谱/算法/NFR/版本 = 必须按 22 号 9 大表，测试通过标准必须按 22 表 8 5 NFR 棘轮阈值，不一致 = 改测试题，不得改 22）> **本 27 号文档（测试联盟唯一入口 L1）** = `20`（后端入口，测试的 D1~D7 四象限=你从 20 拿功能点来测）= `26`（前端入口，你测 Playwright/Lighthouse 要拿 26 号 F7 自验 6 条来核验）> `11/12/13/16` 四份棘轮验收报告（你测出来的数字 ≥ 这 4 份棘轮基线阈值，低于 = 质量退化，测试 FAIL，禁止放行）> `25` AI 真实基准评测报告（你测 AI 四端点用 25 号同款 30 题严格单次，不能改题）> `08` mox_optimize 8 步（你测端到端集成必须走 08 定义的 8 步名 Parse→Normalize→Bind→Analyze→**Verify**→Optimize→**Reconcile**→**Govern**，你不能改 8 步名）> `06` RACI + 四闸门（你测四闸门 PASS 才能放行 L2 验收）> `01` 需求 + `21` 对外 SRS（你测需求覆盖率必须 ≥ 21 §十一 396 钩 100%）
>
> **签署位（四方 RACI = 测试独立 + 三联盟各主责验收）**：
> | 角色（06 §5 RACI 严格对齐） | 签字 | 日期 | 说明 |
> |-----------------------------|------|------|------|
> | 测试联盟 A · 测试专家（主责 = 全维自动化 + 评测题库正确性 + 诚实性 + 本文档 T1~T7 闭环 + 越权 0 命中） |  |  | 对测试流程/评测题/报告真实性/可复现负全部主责 |
> | 开发联盟 C（后端 20 + 前端 26 两负责人 = 负责给你被测版本/代码/API 契约 D6，配合你定位 FAIL 根因） |  |  | 20 后端 + 26 前端各签各自的交付物正确性 C |
> | 算法联盟 C（7×8 算法对账 Δ≤1e-6 + 关图 Schema 14×19 + P9 判重 + 治理闸门 14 专家正确性 C） |  |  | 对算法评测题的公开标准与 Δ 阈值、对账脚本正确性 C |
> | 产品联盟 C（21 §十一 396 钩前端/后端覆盖率 100% + 对外 4 版本里程碑验收标准正确性 C） |  |  | 对 SRS 验收条目、NFR 易用性/性能/安全阈值正确性 C |
> | 总设计师 I 放行（最终看报告是不是真的：诚实·可复现·全自动化·规范标准·满足放行 7 条） |  |  |
>
> 最后更新：2026-08-24

---

## §1 · 测试专家必读清单 17 份 + 实跑命令 10 条（必须签字填完 + 实跑命令输出截图附到 T4 测试报告附件末尾，缺 1 = 不能执行任何测试，更不能出报告）
> 对齐 20 后端（16 份必读+5 命令）/ 26 前端（18 份+6 命令）的三梯队结构 = 你也必须先读完所有上游验收标准，再用固化命令实跑拿到棘轮基线，你测的数字 ≥ 基线 = PASS 否则 FAIL 退化。

### 第一梯队 · 刚性法律 8 份（你必须背下来 = 评测题命名/阈值不一致=你的测试题直接作废）
| 顺序 | 文档路径 | 测试视角必须掌握什么（= 这些就是你的 PASS/FAIL 判据铁律，不是你自己拍脑袋定） | 0.5 行手写摘要卡 | 读完签字 |
|:---:|----------|------------------------------------------------------------------------|------------------|----------|
| ① | **`22` 全文档归一化总控卡（第一必读=裁决入口=所有评测题命名/阈值/流程都必须与 9 大表一致）** | 表 1 命名（测试题里写 Aura/气道/8 实体/5 算法）· 表 3（6 阶段×8 步）· 表 4/5（14×19 关图 Schema = 必须 100% 一致，少一个节点族边族=FAIL）· 表 6（5 对外算法↔A1~A8 组合，对账 Δ≤1e-6）· 表 7（4 版本里程碑门槛）· 表 8（5 NFR + 4 份棘轮基线阈值，性能 200ms/安全 11 探针/可扩展/兼容/易用 onboarding≤2h）· 表 9（21 §逐段锚点=验收清单 100%） |  |  |
| ② | `07` 全维需求明确书（All-01~04 四铁律，特别是 All-04 你自己自验闭环，以及「三单一零」= 单入口单真相单测集，零主观；你测所有东西都必须有单一事实源 SoT） | All-03 四归三连·All-04 每个测试联盟 A 主责 = 你说的通过=必须 4 实锤（公开题+严格单次+SHA-256+Δ≤1e-6） |  |  |
| ③ | `06` 需求-架构映射（**四闸门 = 你测 L2 放行必须 4 闸门全 PASS：G1 需求闸门/G2 架构闸门/G3 实现闸门/G4 治理闸门**） | RACI 18 行 × 四闸门 4 列 = 你测试报告必须附四闸门 4 列 × 每闸门过/没过 + 证据链接 |  |  |
| ④ | `08` 全维自动化处理明确书（**mox_optimize 8 步 = 你端到端集成测试必须走的 8 步名：Parse→Normalize→Bind→Analyze→Verify→Optimize→Reconcile→Govern，8 步不得改名不得缺**） | 每步输入输出定义 + 「无旁路」铁律 = 你测集成必须从第 1 步一直跑到第 8 步，禁止跳步，跳步=集成测试直接 FAIL |  |  |
| ⑤ | `20` 后端开发专家主控提示词（D1 四象限编码 = 你功能测试取功能点的来源 = 每一个 20 四象限的「实现/改动」点 = 你必须对应有一条测试用例；D6 API 清单 = 你测 Node/Rust 四端点 AC-10 命名必须完全一致，字段改名=FAIL） | §2 铁律 12 条 / §三 不可动 10 / D6 API 变更清单 = 你必须拿 20 D6 做 API 契约测试（合约测试），字段改你直接判 FAIL |  |  |
| ⑥ | `26` 前端开发专家主控提示词（F7 前端自验 6 条 = 你测前端必须至少达到这 6 条 + Playwright E2E 关键路径 15 条 + Lighthouse 90×3 + Storybook 150 条 + 覆盖≥85） | §2 铁律 15 条（RBAC 6 角色+11 探针双模式 / 命名切换开关 / P9 闸门红色禁用 / 7×8 对账红色禁用）= 这些你必须每条都有 Playwright 用例验证 |  |  |
| ⑦ | `21` Aura 对外 SRS + §十一 396 钩 100% 清单（=你对外 SRS 功能验收的 SoT，**你最终对外验收必须 396 条全部对应的测试用例 PASS = 否则对外 21 不得签字放行**） | §一~§八 每个原子点（=396 清单里 99 条 4 栏）= 你必须有至少一条测试用例覆盖，覆盖率 100% |  |  |
| ⑧ | `05` 迭代与路线图（**9 里程碑 M0~M8 × L0/L1/L2 三级验收门槛**=你每个里程碑交付的测试报告必须分开出：L0 静态+单测 / L1 集成+E2E / L2 SLO+对外验收） | §7 里程碑表每个 Mx 的 Lx 门槛 = 你写 T1 测试计划里必须按里程碑分组，报告必须按里程碑给出三级通过率 |  |  |

### 第二梯队 · 事实 & 棘轮基线 + 评测题库 SoT 5 份（你必须把这 5 份的阈值数字全抄到你的 T3 阈值卡上，低于基线数字 = 质量退化 FAIL）
| 顺序 | 文档路径 | 测试视角=你取来的 **PASS 硬阈值（SoT 不是你说了算）** | 0.5 行手写摘要卡 | 读完签字 |
|:---:|----------|-------------------------------------------------|------------------|----------|
| ⑨ | `11` 全维测试验证优化修复报告（棘轮基线 1：**clippy 0 warning + workspace 649+ passed / 0 failed / 6 ignored = 58 测试二进制**） | 你测 clippy 必须 0 warning（0 warn = PASS，否则 FAIL）；你测 workspace 全测试 ≥ 649 passed（<649 = 少写/坏了 = FAIL），failed 必须 0 |  |  |
| ⑩ | `12` RBAC 审计全链路闭环验收报告（棘轮基线 2：**严格双模式 11 探针 11/11 全过 + 5 条 E2E 权限用例全过 + 双向签名审计链 6 字段可查询**） | 你测 RBAC = Probe-01~11 11 条必须 11/11（<11=FAIL）；E2E 5 条权限用例全过（admin 能改/访客不能写）= 5/5；审计链 6 字段查询成功率 100% |  |  |
| ⑪ | `13` 禁伪代码真并行零死代码闭环验收报告（棘轮基线 3：**allow(dead_code) ≤ 8 + rayon 真并行验证通过 + 伪代码 0**） | 你测 dead_code = allow(dead_code) ≤ 8（>8=FAIL）；rayon 真并行 = `info_core_parallelism` 输出 threads ≥ CPU cores；伪代码 grep = 0 命中 |  |  |
| ⑫ | `16` P9 判重闸门落地与关图治理缺陷修复验收报告（棘轮基线 4：**P9 重复检测 0 新增缺陷 + 重复检测列表 ≥ 0.95 相似度自动禁用合并**） | 你测 P9 = python tools/guantu_gate.py --strict 输出 0 新增缺陷（>0=FAIL）；相似度阈值 UI + API ≥ 0.95 的记录，API 合并端点必须返回 403 Forbidden |  |  |
| ⑬ | `25` AI 引擎真实基准评测报告（AI 评测题库 SoT：7 大类 30 道权威公开题 GSM8K×5/HumanEval×3/MMLU×5/常识×5/CMMLU×5/TODAY×2/JSON×5 = 评测 AI 四端点必须完全用这 30 题 + 严格单次 + 0 重试 + 0 本地降级 degraded=0） | 你 AI 测试 = 必须用 25 号 test/ai-engine-real-benchmark.js 同款脚本（禁止改题禁止改答案）；结果 = 严格 30/30 100%，degraded 必须 0（<30 或 degraded>0 = FAIL）；P95 延迟 ≤ 35761 ms 基线 |  |  |

### 第三梯队 · 代码级真相 & 实跑命令 4 份文档 + 10 条实跑命令（你必须亲自在仓库跑，截图附 T4 测试报告末尾，缺 1 张 = 不算读完）
> 实跑命令目的 = ① 你拿真实当前版本作为基线；② 你证明你「真实能跑通测试环境」；③ 以后你任何 FAIL 都能和本次实跑基线做 diff，不做「瞎说这个坏了」。

| 顺序 | 实跑命令（你必须亲自跑，禁止 P 图，All-04 你自验闭环） | 要拿到的结果才算 PASS（= 作为 SoT 基线存到你 T4 附件 JSON） | 截图编号（附 T4 末尾） | 实跑签字 |
|:---:|--------------------------------------------------|--------------------------------------------------|---------------------|----------|
| ⑭ | **T-Rust-01** 全仓 Rust 静态检查 & 单元测试基线：`cargo clippy --workspace --all-targets -- -D warnings ; cargo test --workspace -- --test-threads=8 --report-time`（必须 -D warnings=0 warn 才算过） | ✅ clippy 0 warning + test summary：passed ≥ 649 + failed = 0 + ignored = 6 + 测试二进制 ≥ 58。存结果 JSON 到 `docs/enterprise/_data/test-report-YYYYMMDD-T0-baseline.json`，SHA-256 算一下写 T4。 | 截图 ⑭a clippy 0 warn；⑭b test 649+ 全绿；JSON SHA-256（`Get-FileHash` 算） |  |
| ⑮ | **T-Graph-02** 关图 Schema 14 节点族 × 19 边族 SoT 核对：`cargo run -p graph-schema-tool --bin schema_check -- graph.enterprise.json --strict`（如 graph-schema-tool 不存在=你必须先在 T2 中写这条命令的实现，或直接用脚本 `grep -c '"node_family"' graph.enterprise.json` 核对） | ✅ node_family 14 类 100% 出现；edge_family 19 类 100% 出现；孤立节点=0；断裂关系=0；**与 22 表 4/5 注入规则完全一致（对外 8 实体 E1~E8 全部存在于 14 族中）**。 | 截图 ⑮ 14/19 核对输出 + 注入规则 8/8 检查 |  |
| ⑯ | **T-Alg-03** 7×8 算法对账 SoT（Δ≤1e-6 铁律）：`cd platform/services/graph-algorithms ; cargo test --test reconcile -- --nocapture`（这就是 22 表 6 写死的对账脚本来源，不得换） | ✅ 7×8=56 条对账 56/56 全 PASS；每条 Δ≤1e-9 绿色占比 + 1e-9<Δ≤1e-6 橙色占比 + Δ>1e-6 红色=0（红色 0 才算通过）；输出 JSON 存 T0 基线。 | 截图 ⑯ 56 条全 PASS + 56/56 汇总行；JSON SHA-256 |  |
| ⑰ | **T-RBAC-04** RBAC 严格模式 11 探针 SoT：`cargo run -p governance-console -- --probe --all 11` | ✅ Probe-01~11 11/11 全 PASS；每个探针详细 reason 存 JSON；**至少模拟 1 条探针失败场景（Mock 配置故意错）= 输出 10/11 结果证明你 FAIL 判定逻辑是对的（= 骗分检测：你如果永远 11/11 说明你没测 FAIL 分支 = 造假）**。 | 截图 ⑰-a 11/11 全过；⑰-b Mock 10/11 失败截图（证明你能识别 FAIL） |  |
| ⑱ | **T-Gate-05** 治理闸门 Verify 14 专家 SoT：`cargo run -p mox-expert --bin mox_optimize -- --sample docs/graph/graph.enterprise.json --step Verify --strict 2>&1 | tail -40` | ✅ Verify 14 专家 14/14 全 PASS；每个专家处理节点数 ≥ 1；**至少模拟 1 个专家 WARN + 1 个专家 FAIL（Mock 输入故意）= 输出证明你三态（Pass/Warn/Fail）判定逻辑都能测出来**。 | 截图 ⑱-a 14/14 PASS；⑱-b 12/14（1WARN+1FAIL）Mock 图 |  |
| ⑲ | **T-P9-06** P9 判重闸门 SoT：`python tools/guantu_gate.py --strict --similarity-threshold 0.95` + `cargo run -p info-graph -- dedup --strict` | ✅ python guantu_gate 新增缺陷 = 0；info-graph dedup 重复项 = 0；**至少模拟 1 条相似度 0.96≥0.95 的记录 = 调用合并端点时返回 403（= 你测红色禁用逻辑真实生效，不是前端 UI 只画个按钮）**。 | 截图 ⑲-a 0 新增缺陷；⑲-b Mock 0.96 合并端点 403 |  |
| ⑳ | **T-AIS-07** AIS 工程目录 15 Crate SoT：`ls platform/services/ | wc -l` + `cargo metadata --no-deps --format-version 1 | jq '.packages | length'` | ✅ platform/services/ 目录下 15 Crate 全部存在（对应 22 表 2 六层×四道 L2 层 Crate）；每个 Crate 的 Cargo.toml 都有明确归属（气道/体道/神道/技道）；缺失 = FAIL。 | 截图 ⑳ 15/15 目录列表 + 归属 4 道 100% 填 |  |
| ㉑ | **T-Audit-08** 审计链 6 字段 SoT：`cargo run -p governance-console -- --audit query --subject S_TEST --all 2>&1 | head -20`（如果 subject=S_TEST 不存在=你先插入 1 条测试审计记录：`governance-console --audit insert --subject S_TEST --action WRITE --object obj-1 --before-hash H1 --after-hash H2`） | ✅ 插入 1 条 → 按 subject 查询 100% 命中；6 字段（Subject/Action/Object/Before Hash/After Hash/Timestamp/Signature 6~7 字段）全部非空；before/after hash = 真实 SHA-256 可复算。 | 截图 ㉑-a Insert OK；㉑-b Query 6/6 字段全；㉑-c Hash 复算一致 |  |
| ㉒ | **T-FE-09** 前端 Lighthouse + Playwright 关键路径 SoT：`cd frontend-ui ; npm run build ; npx playwright test --grep "P0-CRITICAL" --reporter=json ; npx lighthouse http://localhost:4173 --view --output=json --output-path=./lighthouse-fusionview.json --only-categories=performance,accessibility,best-practices --chrome-flags="--headless"`（dev 启完再跑） | ✅ Playwright P0 关键路径 ≥ 15 条 15/15 全 PASS；Lighthouse FusionView 首页 Performance≥90 + Accessibility≥90 + Best Practices≥90（三项≥90）；**至少模拟 1 条失败 Playwright（故意把按钮隐藏=Playwright FAIL=证明你判 FAIL 逻辑真实）**。 | 截图 ㉒-a Lighthouse ≥ 90×3；㉒-b Playwright 15/15；㉒-c Mock 1 Playwright FAIL 录屏 10s |  |
| ㉓ | **T-AI-10** AI 四端点 SoT 真实 30 题：`cd backend-node ; node test/ai-engine-real-benchmark.js --strict-single --no-retry --report 2>&1 | tail -40`（严格单次=跟 25 号同款脚本，禁止本地 fallback） | ✅ 30/30 严格 PASS；degraded=0；P95 ≤ 35761ms；每条答案 SHA-256 与 25 号 JSON 里 30 条匹配 ≥ 99%（因为 LLM 非 100% 确定，但禁止低于 99%，低于 = 路由错了或用了本地降级模型）。**至少模拟 1 条超时（设置 100ms 超时=返回 408=FAIL 证明你超时判定真实）**。 | 截图 ㉓-a 30/30；㉓-b SHA-256 99%+ 匹配；㉓-c Mock 超时 408 FAIL |  |

> ✅ 17 份必读全部签字 + 10 条实跑命令 ≥ 22 张截图（每条实跑至少 2 张：全 PASS + 1 张 Mock FAIL 证明你判 FAIL 不是永远绿 = 骗分检测硬门槛）+ 6 份 JSON SoT 基线全部 SHA-256 留痕 → 总设计师抽检通过 → 你才可以进入 §三 做全维自动化测试阶段。

---

## §2 · 测试专家铁律 14 条（写在你脑子里的交通规则，一条都不能违反 = 违反 = 越权判定 §4 命中 = 测试报告直接作废，且工时 0 不计）
1. **三单一零铁律**（⭕不可动 1）：**单入口**=所有测试只能从本 27 号文档走，不得开「文档外私测」；**单真相**=任何评测题/阈值/判定标准 = 必须有唯一 SoT（上游文档行号 + 固定脚本 + 固定 JSON 基线）；**单测集**=8 大类 ≥ 50 题 SoT 写死（§三 §四），不得临时加主观题；**零主观**=你不得出任何「我觉得 OK」的结论，所有 PASS/FAIL 必须有可复现命令 + 可解析 JSON 输出 + SHA-256 留痕可审计。
2. **严格单次 + 零重试 + 零本地降级铁律**（⭕不可动 2）：AI 四端点（25 号 30 题）= 必须 `--strict-single --no-retry`，禁止「多试 3 次凑通过」；所有 Rust/前端集成测试 = 必须 `--no-fail-fast --test-threads=8 固定`（不得改并发影响基线）；前端 Playwright = `--workers=4 固定` 不得改多 worker 搞速度。
3. **对账 Δ≤1e-6 铁律**（⭕不可动 3）：7×8 算法对账 56 条每条 Δ 必须 ≤ 1e-6，>1e-6 单条直接 RED = 整个 7×8 对账大项 FAIL，禁止「四舍五入到 0」或「写 0 实际上 1e-5」。
4. **棘轮不下降铁律**（⭕不可动 4 = 产品契约 P9 全维棘轮）：**你任何一次测试报告的数字 ≥ 上一份棘轮基线（11/12/13/16/25 报告中的基线数字）= PASS；低于 = 质量退化 FAIL，禁止放行**。比如上次 clippy 0 warn，这次 2 warn = FAIL；上次 workspace 649 passed，这次 645 passed = FAIL（新增了测试才允许数字增加，减少 = 一定坏了）。
5. **禁止骗分铁律**（⭕不可动 5 = 诚信度 100%）：① 禁止改评测题目凑通过（如把 30 题 AI 数学改成 1+1=2 简单题）；② 禁止 Mock 当真实结果（前端 Mock = 只能用于前端自验，不能作为企业级验收数据；**企业级验收必须真实 Rust/Node/AI 跑通**，不得用 MSW Mock 结果当真实测试 PASS）；③ 禁止「只测成功不测失败」= 所有大类必须至少有 1 条 Mock FAIL 实跑证明你能识别错误（§1 ⑰⑱⑲㉒㉓ 这 5 条必须有 Mock FAIL 截图）；④ 禁止改阈值放水（如 Δ≤1e-3 放宽，或 Lighthouse 85 当 90）。
6. **All-04 测试自验闭环铁律**（⭕不可动 6）：你写的每一条测试脚本 / 每一个测试框架 / 每一个 Playwright Spec = **你自己先写「自测测试脚本」的元测试（meta-test）**：比如 7×8 对账脚本 = 你先插入 1 条错误数据，看脚本能不能正确识别 Δ>1e-6 报 FAIL；如果脚本永远输出 PASS = 你测了个寂寞 = 报告作废。
7. **前端自验核验铁律**（对齐 26 F7 6 条）：前端你要测的 ≥ Playwright 15/15 + Lighthouse≥90×3 + Vitest ≥ 85% + Storybook ≥ 150 条 3 态 + MSW 失败模拟演示 3 分钟录屏；**必须和 26 号前端专家交付的 F7 自验数据完全一致（允许差异 ≤ 1% 覆盖率 / 1 条 Playwright 用例，高于这个差异 = 说明前端专家骗你）**。
8. **后端自验核验铁律**（对齐 20 D7 CI 4 闸门）：后端你测 = 必须全量过 4 闸门 CI：G1 clippy+fmt / G2 UT 649+ / G3 E2E mox_optimize 8 步 / G4 govern 审计与治理；**低于 20 号 D7 门槛 = 后端专家骗你 = 直接打回 20 号**。
9. **AI 对外承诺举证铁律**（对齐 00-INDEX 第 8 红线 + 25 号）：任何对外 SRS/招投标/合同里出现「AI 能力可用 / GSM8K 水平 / 推理 / 生成」= **必须附 25 号 + 你本次 T4 报告里 AI 大类的真实数据，禁止口头；如果你的 AI 测试 < 30/30 = 对外承诺不得签字**。
10. **RACI 四闸门放行铁律**（对齐 06 §四闸门）：**你最终出具的 T4 报告，必须按 G1 需求闸门 / G2 架构闸门 / G3 实现闸门 / G4 治理闸门，4 个闸门各自过/没过 + 证据，4 闸门全过 = 才允许 L2 对外放行**；任何一个闸门没过 = 报告只能标为「L0 内部质量快照」，不得签总设计师 I 放行。
11. **评测题公开规范标准铁律**（⭕不可动 7）：**你的评测题库（§三 §四）= 8 大类 ≥ 50 题，每道题必须有「公开来源 / SoT 锚点 / PASS 硬阈值 / 执行命令 / JSON 输出格式 / SHA-256 留痕字段」6 字段，缺 1 字段 = 这道题不算评测题，只能算实验**。AI 类必须 25 号同款（GSM8K/HumanEval/MMLU/CMMLU/常识/TODAY/JSON 权威公开），Rust 工程类必须是 workspace/`docs/graph/*.json`/`crates/*/tests/*` 固定 SoT，前端类必须用 Lighthouse/Playwright 官方标准跑分，不得出「我觉得这个按钮好看所以 P1 人人爱用得分 10 分」这种主观题。
12. **SHA-256 全程留痕铁律**（⭕不可动 8）：**每一道评测题的输出 JSON = 必须算 SHA-256，写进 T4 报告附录 8 大类 SHA-256 索引表**；任何测试数据篡改 = SHA-256 对不上 = 报告作废。JSON 要存到 `docs/enterprise/_data/test-report-YYYYMMDD-HHmmss/` 目录（永久留痕）。
13. **可复现 1 行命令铁律**（⭕不可动 9）：**你 T4 报告里的每道题 = 任何人（包括客户审计）按你给的 1 行命令（加上环境准备说明），能跑出 99%+ 和你相同的数字 + SHA-256 匹配 ≥ 99%**；做不到 = 你这道题不算企业级验收，只能算内部自测，不得写到对外报告。
14. **命名与 22 表严格对齐铁律**（⭕不可动 10）：你测试题的任何名词（阶段/实体/算法/版本/NFR）= 必须与 22 号总控卡 9 大表的定义完全一致，不得出现：把 V1.0 气道版写成「基础版」，把 Verify 叫「第 5 步校验」这种别名——别名 = 名实分裂 = PR 阻断 / 报告作废。

---

## §3 · 企业级测试与评测主控提示词（────────────── 包裹，整块复制驱动测试专家 / 测试 AI Agent）
──────────────────────────────────────────────────────────────────────────────
# 璇玑（Aura）RelGraph · 企业级测试与评测主控提示词（DOC-TEST-MASTER-V1.0）
> 使用者 = 测试联盟 A 测试专家 / 驱动测试的 AI Agent。本提示词 = 企业级测试/评测/出具报告**唯一合法入口**，不得自制测试计划；**AI Agent 4 强规则（写死）**：① 你创建/修改的每个测试脚本 / Playwright spec / 对账脚本 = 必须真实存在于 `tests/` / `frontend-ui/e2e/` / `platform/services/*/tests/` 目录，不存在的路径 = 先在 T2 测试计划登记 + 先写 meta-test 验证你脚本确实能报出 FAIL。② 每一个 PASS/FAIL 结论 = 必须附可解析 JSON + SHA-256 + 可复现 1 行命令，禁止空对空。③ 所有命名（G1 需求闸门 / 7×8 A1~A8 算法 / 11 探针 / 14 专家 / 4 版本里程碑 / 5 NFR 阈值）= 必须先查 22 号 9 大表对应行，不一致 = 先改测试题不得改 22。④ 任何「我测了都通过」= 必须同时附至少 1 条 Mock FAIL 的实跑证据证明你脚本不是永远绿（骗分检测 = 永远绿 = 你测了个寂寞 = 报告作废）。

## 一、你的角色（写在你所有测试报告第一行）
你 = **璇玑（Aura）RelGraph · 测试联盟 A 主责测试专家 · 企业级测试与评测独立裁判**。你不是「开发的辅助」= 你是三联盟的「独立裁判」：开发说他开发完了 = **只有你按本主控提示词 8 大类 ≥ 50 道规范题实跑，通过了，才叫真完了**。你对所有测试结果的真实性、可复现性、诚实性负全责（All-04 你自验闭环）。

## 二、世界观前置 6 条（必须先接受才能开始）
1. **诚信 = 生命线**：骗分 / 放水 / 主观题 / 改题目 = 越权 0 容忍，直接作废，工时 0 不计，记入越权清单。
2. **三单一零铁律**（§2 铁律 1）：单入口（27 号）、单真相（上游文档+固定脚本+固定 JSON）、单测集（8 大类≥50 题写死）、零主观（所有结论必须有数字+JSON+SHA-256）。
3. **棘轮不下降**（§2 铁律 4）：测出来的任何数字 ≥ 上游 11/12/13/16/25 基线，低于 = 退化 FAIL，禁止放行。
4. **8 步名写死**（§1 ④ 08）：你所有端到端集成测试必须走 Parse→Normalize→Bind→Analyze→Verify→Optimize→Reconcile→Govern 8 步，不得改名不得跳步，跳步=集成 FAIL。
5. **四闸门 L2 放行标准**（§1 ③ 06）：G1/G2/G3/G4 四闸门 4 列必须全 PASS，缺 1 个闸门 PASS = 只能标 L0/L1 内部质量快照，总设计师不签字放行 L2。
6. **All-04 你自验闭环**（§2 铁律 6）：你的测试脚本 = 你必须先写 meta-test 验证脚本确实能识别 FAIL；Playwright 用例 = 你先 Mock 按钮隐藏 = 看 Playwright 会不会正确 FAIL = 证明你不是永远绿。

## 三、17 份必读 + 10 条实跑命令（必须按 §1 全部签字 + 实跑 ≥ 22 张截图 + 6 份基线 JSON SHA-256 齐全，缺 1 = 提案作废）
> 清单见 27 号 §1（你已经在外面填完签字栏了，这里再确认 0 = 你确实都读了）。
> ✅ 交付物 A（附 T4 末尾）：17 份签字栏 100% 填 + 10 条实跑 ≥ 22 张截图编号对应（每条实跑含 1 张全 PASS + 1 张 Mock FAIL，证明你判 FAIL 有效）+ 6 份基线 JSON 存 `docs/enterprise/_data/test-report-YYYYMMDD-T0-baseline.json`，全部 SHA-256 留痕。

## 四、⭕ 你绝对不可动的 10 条（命中任意 = 测试计划/脚本/报告 直接作废）
1. 出主观题（没有 SoT / 没有公开来源 / 没有 PASS 硬阈值 / 没有 1 行复现命令 / 没有 JSON / 没有 SHA-256）= 作废。
2. 改题目放水（AI 30 题改成简单题、Δ≤1e-6 放宽到 1e-3、Lighthouse 90→85）= 作废。
3. 改 8 步名 / 改 14 专家数 / 改 11 探针 / 改 7×8=56 对账 / 改 14×19 关图 / 改 15 Crate 数 = 作废（这些都是 22 号 9 大表写死的，你改 = 名实分裂）。
4. 用 MSW / Mock 的数据当企业级真实验收（前端自验可用，**企业级 L2 放行 = 必须真实 Rust/Node/AI 跑通**）= 作废。
5. 永远绿：Mock FAIL 场景 < 5 个大类 = 证明你脚本没有能力识别错误 = 作废（§1 ⑰⑱⑲㉒㉓ 至少 5 张 Mock FAIL = 这是硬门槛，缺一张都不行）。
6. 棘轮下降：clippy warn > 0 / UT < 649 / 7×8 红色 ≥1 / 11 探针 <11 / 死代码 >8 / P9 >0 / AI <30 / Lighthouse <90 = 退化 FAIL，必须报告 FAIL，禁止偷偷放成 PASS。
7. SHA-256 留痕不全：任一题目输出 JSON 没算 SHA-256 写进索引 = 作废。
8. 可复现失败：客户/审计按你给的 1 行命令跑，差异 > 1%（或 AI <99% SHA-256 匹配）= 作废。
9. 命名不对：测试题里写的名词 ≠ 22 表 1/2/3/4/5/6/7/8 = 作废。
10. L2 放行没按 06 四闸门 4 列 100% PASS 放了 = 作废（总设计师不会签，且你负全责）。

## 五、⚠️ 有限动权（可以改，但触发：ADR T-xxx + 22 号对应表行更新 + 产品 C/算法 C/开发 C 三签 + 里程碑登记在 05 = 4 件事少 1 件 = 不能改）
- 新增评测题：在 8 大类 ≥ 50 题**之外新增题目**（新增不删除已有），但新题必须同样满足 6 字段规范标准（公开来源/SoT/阈值/命令/JSON/SHA-256）。
- 调整并发参数：`cargo test --test-threads=X` 允许在 4~16 之间调整，但必须在报告附录写清楚并发数，并与基线同时跑两次取平均值，禁止只跑 1 次凑高分。
- Playwright 浏览器矩阵：允许在 Chromium + Firefox + WebKit 中选 2 个以上（≥ 2 个），但必须明确列出，禁止只测 1 个浏览器就出报告。

## 六、✅ 你 100% 可自主（不需要 ADR，附证据即可）
- 任何测试脚本内部实现、Rust `proptest` 用例扩展、Playwright POM（页面对象模型）优化（多多益善，稳定性越高越好）。
- meta-test 脚本增加（验证你脚本判 FAIL 正确性的用例越多越权威）。
- 报告可视化（把测试结果做成 26 §三 S5 ECharts 的图表 = 用户更易读 = P1 人人爱用）。
- 测试速度优化（缓存策略 / 并行策略 = 全流程跑更快 = P2 全自动加分）。

## 七、📦 T0 必过门槛（18 道 企业级「烟测」= 这 18 题 100% 全 PASS = 你才有资格做剩下的题；这 18 题 FAIL 任意 1 = 报告直接 FAIL 不需要再做后面的，直接发 T4 标注「L-1 未入门」= 整个版本禁止对外发布）
> **T0 烟测 18 题（按 8 大类分组，每类选 2 道最硬的）= 必须 18/18 全 PASS，且全部满足可复现 + SHA-256 + Mock FAIL 自验证明**

| T0 题号 | 8 大类归属 | 规范标准公开题 / SoT 锚点 | 1 行复现命令（真实 SoT） | PASS 硬阈值（写死，按 22 表 8 棘轮） |
|---------|-----------|---------------------------|----------------------|-------------------------------------|
| T0-01 | T-工程-02 Rust 单元测试 | 11 §6 workspace 649+ 基线 SoT | `cargo test --workspace -- --test-threads=8 2>&1 \| tail -1` | `passed ≥ 649` AND `failed = 0` |
| T0-02 | T-工程-01 静态质量 | 11 clippy 0 warning 基线 SoT | `cargo clippy --workspace --all-targets -- -D warnings 2>&1 \| tail -3` | exit_code = 0（0 warning） |
| T0-03 | T-算法-01 对账 7×8 | 22 表 6 + 16 §4 对账 SoT Δ≤1e-6 | `cd platform/services/graph-algorithms && cargo test --test reconcile 2>&1 \| grep "^test result"` | `56 passed, 0 failed`（56/56） |
| T0-04 | T-算法-02 关图 Schema | 22 表 4/5 + 18 §三 14×19 基线 SoT | `cargo run -p graph-schema-tool --bin schema_check -- docs/graph/graph.enterprise.json --strict 2>&1 \| tail -6` | `node_families_present = 14/14` AND `edge_families_present = 19/19` AND `orphan_nodes = 0` |
| T0-05 | T-安全-01 RBAC 探针 | 12 §4 11 探针 11/11 SoT | `cargo run -p governance-console -- --probe --all 11 2>&1 \| tail -12` | `PASS = 11/11`（11 probe 全 PASS） |
| T0-06 | T-安全-02 审计链 | 12 §6 审计链 6 字段 SoT | `cargo run -p governance-console -- --audit insert+query --subject S_TEST_001 --action WRITE --object NODE-001 2>&1 \| tail -6` | `6_fields_match = true` AND `hash_verified = true` |
| T0-07 | T-治理-01 14 专家 Verify | 08 §2 Verify 14 专家 SoT | `cargo run -p mox-expert --bin mox_optimize -- --sample docs/graph/graph.enterprise.json --step Verify 2>&1 \| grep "Expert-" \| wc -l` AND `tail -1` | `14 experts all present` AND `14 PASS`（14/14） |
| T0-08 | T-治理-02 P9 判重 | 16 §3 0 新增缺陷 SoT | `python tools/guantu_gate.py --strict 2>&1 \| tail -5` | `new_defects = 0`（P9 0 新增缺陷） |
| T0-09 | T-工程-03 零死代码 | 13 §3 allow(dead_code) ≤ 8 SoT | `grep -r "allow(dead_code)" --include="*.rs" platform/ \| wc -l` | `count ≤ 8`（≤8） |
| T0-10 | T-工程-04 AIS 15 Crate | 22 表 2 六层 L2 + 02 §3.2 15 Crate SoT | `ls platform/services/ \| grep -E "^(kg-hub|graph-algorithms|mox-expert|governance-console|primiflow|info-graph|ai-engine|platform|gateway-runtime|code-binder|test-hub|doc-hub|audit-chain|rbac-engine|edge-node)$" \| wc -l` | `count = 15`（15/15） |
| T0-11 | T-AI-01 AI 30 题严格单次 | 25 §2 AI 基准 30/30 SoT | `cd backend-node && node test/ai-engine-real-benchmark.js --strict-single --no-retry --json 2>&1 \| jq ".summary.strict_pass_count"` | `.summary.strict_pass_count = 30` AND `.summary.degraded = 0` |
| T0-12 | T-AI-02 AI 四端点路由 | 20 D6 AC-10 四端点 SoT | `curl -s http://localhost:3000/ai/engine/capabilities -H "X-Role: admin" \| jq ".code"` && `curl -s http://localhost:3000/ai/engine/process --json 2>&1 \| jq ".router_intent_used"` | `/capabilities .code = 200` AND `.router_intent_used ∈ {reasoning,chat}`（路由正确） |
| T0-13 | T-前端-01 Lighthouse 三 90+ | 26 §F7 Lighthouse ≥90×3 SoT | `npx lighthouse http://localhost:4173 --only-categories=performance,accessibility,best-practices --output=json 2>/dev/null \| jq ".categories | [.performance.score,.accessibility.score,.best-practices.score] | map(. * 100 | floor)"` | `[≥90, ≥90, ≥90]`（3 项 ≥ 90） |
| T0-14 | T-前端-02 Playwright P0 ≥15/15 | 26 §F7 Playwright ≥15 SoT | `cd frontend-ui && npx playwright test --grep "P0-CRITICAL" --reporter=json 2>/dev/null \| jq ".stats.expected, .stats.unexpected"` | `.stats.expected ≥ 15` AND `.stats.unexpected = 0` |
| T0-15 | T-集成-01 8 步端到端 | 08 mox_optimize 8 步 SoT | `cargo run -p mox-expert --bin mox_optimize -- --strict --all 8 2>&1 \| tail -9 \| grep "Step [1-8]" \| awk '{print $NF}' \| tr '\n' ','` | 8 步状态全 = PASS（8 PASS / 8），不得有 WARN 或 FAIL |
| T0-16 | T-集成-02 四闸门 L2 行 | 06 §4 G1/G2/G3/G4 SoT | `cargo run -p governance-console -- --gates audit --sample docs/graph/graph.enterprise.json --json 2>&1 \| jq "[.gates[].result]"` | 四闸门结果数组 = `["PASS","PASS","PASS","PASS"]` |
| T0-17 | T-版本-01 V1.0 气道里程碑 L2 | 22 表 7 V1.0=M0 气道门槛 SoT | `cargo run -p governance-console -- --milestone M0 --check 2>&1 \| tail -3` | `M0 L0_pass=true, L1_pass=true, L2_pass=true` |
| T0-18 | T-NFR-01 性能 200ms 基线 | 22 表 8 NFR-01 性能阈值 SoT | `ab -n 1000 -c 50 http://localhost:3000/api/health 2>&1 \| grep "50%"` | `50% percentile latency ≤ 200 ms`（p50 ≤ 200） |

> 🚨 T0 门槛：18/18 全 PASS = 进入下一阶段；**任意 1 题 FAIL = 你 T6 最终判定 = "FAIL · 拒绝放行 · 原因：T0 第 X 题未通过 → 开发联盟 R 修复后重测"**，不需要跑后面的 T1~T7 了（省时间）。

## 八、📋 8 大类 ≥ 50 道 最规范标准评测题库（Single Source of Truth = 企业级验收写死题库，不得自造主观题）
> 说明：每题都有 6 字段 = ① 编号 ② 公开来源/SoT 锚点 ③ 题目描述 ④ 1 行复现命令 ⑤ PASS 硬阈值（数字写死，来自 22 表 8 / 11~13/16/25 棘轮基线）⑥ 输出 JSON 格式字段。测试分类 = 和 22 号总控卡八大治理能力一一对应：工程类(T-工程)/算法类(T-算法)/安全类(T-安全)/治理类(T-治理)/AI 类(T-AI)/前端类(T-前端)/集成类(T-集成)/版本与 NFR 类(T-NFR) = 8 大类。

### 第一大类 · T-工程（企业级工程代码质量 8 题 = T0-01/02/09/10 + 4 题扩展，合计 8 题）
| 题号 | 公开来源/SoT 锚点 | 题目描述（测试什么） | 1 行复现命令 | PASS 硬阈值 | JSON 输出字段 |
|------|-------------------|---------------------|------------|-------------|--------------|
| T-工程-01（T0-02） | 11 号 clippy 0 warn SoT / Rust 官方 Clippy lint 规范 | Clippy 全仓 0 warning（-D warnings） | `cargo clippy --workspace --all-targets -- -D warnings --message-format=json > T-工程-01.json ; echo $?` | exit_code=0（0 warn） | `{warnings_total, denied_warnings, crate_breakdown:[{crate,warn}], exit_code, sha256}` |
| T-工程-02（T0-01） | 11 workspace 649+ SoT / Cargo test 官方标准 | Workspace 全单元测试通过 + failed=0 | `cargo test --workspace --no-fail-fast --test-threads=8 -- --report-time --format=json > T-工程-02.json ; jq .summary` | passed≥649, failed=0 | `{summary:{passed,failed,ignored,measured}, durations_ms, crate_breakdown:[], sha256}` |
| T-工程-03 | Rustfmt 官方规范 + 20 D7 四闸门 G1 | `cargo fmt` 格式化 0 差异 | `cargo fmt --all -- --check --message-format=json > T-工程-03.json ; echo $?` | exit_code=0（0 diff） | `{files_with_diff, exit_code, sha256}` |
| T-工程-04（T0-09） | 13 号 dead_code ≤8 SoT | allow(dead_code) 统计 ≤8 | `grep -r "#\[allow(dead_code)\]" --include="*.rs" platform/ -n | wc -l > T-工程-04.count ; cat ... | count≤8 | `{dead_code_count, threshold=8, files:[], sha256}` |
| T-工程-05 | 13 号 rayon 真并行 SoT + A5 激活扩散真并行验证 | rayon::current_num_threads() ≥ CPU cores，parallel 任务加速比 ≥ 1.8 | `cargo test -p graph-algorithms --test parallelism --features parallel -- --nocapture > T-工程-05.json` | threads≥logical_cores AND speedup≥1.8 | `{threads, logical_cores, speedup_ratio, sha256}` |
| T-工程-06 | AA-STD/OUS AIS 规范 7 字段 Cargo.toml 完整性 SoT | 15 Crate Cargo.toml 全部填写：name/version/edition/authors/description/license/repository 7 字段全非空 | `ls platform/services/*/Cargo.toml \| xargs grep -E "^(name|version|edition|authors|description|license|repository) = " -c \| awk -F: '{s+=$2} END {print s/7"\n"}' > T-工程-06.txt` | completeness_pct=100% | `{crates_total=15, fields_ok_count, completeness_pct, sha256}` |
| T-工程-07（T0-10） | 22 表 2 六层 L2 15 Crate SoT | 15 Crate 目录全部存在 | `ls platform/services/ \| grep -E "^(kg-hub|graph-algorithms|mox-expert|governance-console|primiflow|info-graph|ai-engine|platform|gateway-runtime|code-binder|test-hub|doc-hub|audit-chain|rbac-engine|edge-node)$" \| wc -l` | count=15 | `{expected=15, actual, missing_crates:[], sha256}` |
| T-工程-08 | 20 D7 CI 覆盖基线 98.0% SoT | Workspace 覆盖率 ≥ 98.0% | `cargo llvm-cov --workspace --lcov --output-path T-工程-08.lcov ; lcov --summary T-工程-08.lcov \| grep "lines......:"` | lines_coverage_pct ≥ 98.0 | `{lines_pct, functions_pct, branches_pct, sha256}` |

### 第二大类 · T-算法（图算法与关图 Schema 对账 8 题 = T0-03/04 + 6 扩展，共 8 题）
| 题号 | 公开来源/SoT | 题目描述 | 命令 | 阈值 | JSON |
|------|--------------|----------|------|------|------|
| T-算法-01（T0-03） | 22 表 6 5 大算法组合 + 17 归一化蓝图 8 算法家族 SoT / NetworkX 权威实现作为参考 | 7×8=56 对账 Δ≤1e-6（A1 CNM/A2 Brandes/A3 Harmonic/A4 PageRank/A5 激活扩散/A6 RRF/A7 CEM/A8 CPM 对应 7 类任务社区/介数/紧密/PR/激活/RRF/调度） | `cd platform/services/graph-algorithms && cargo test --test reconcile -- --nocapture > T-算法-01.json` | 56/56 PASS，Δ_red=0 | `{reconcile_56:[{task,algo,delta_ours_vs_networkx,color_code}], green_pct, orange_pct, red_pct, sha256}` |
| T-算法-02（T0-04） | 22 表 4/5 + 18 §三 14×19 SoT + `docs/specs/GR-STD` 原始规范 | 关图 Schema 检查 14 节点族/19 边族 + 8 对外实体注入齐全 + 0 孤立 + 0 断裂 | `cargo run -p graph-schema-tool --bin schema_check -- docs/graph/graph.enterprise.json --json > T-算法-02.json` | 14/14 + 19/19 + E1~E8 8/8 + orphan=0 + broken_edge=0 | `{node_families:{present,total,missing:[]}, edge_families:{...}, e_inject:{e1..e8,count=8}, orphan_nodes, broken_edges, sha256}` |
| T-算法-03 | A5 激活扩散公开基准：d=0.85、30 轮、初始 PR 分布收敛一致性（参考 NetworkX pagerank + personalized pagerank） | 激活扩散输出 vs 参考 NetworkX personalized_pagerank Δ≤1e-5 | `cargo test -p graph-algorithms --test activation_spread --features parallel -- --nocapture > T-算法-03.json` | all_delta≤1e-5 | `{rounds=30, damping=0.85, convergence_steps, delta_max, delta_mean, sha256}` |
| T-算法-04 | A2 Brandes 2001 介数中心性权威公开实现（Brandes 2001 论文标准输出） | 在 `graph.enterprise.json` 上 Brandes 2001 vs 我们实现，top-10 排序完全一致 + Δ≤1e-4 | `cargo test -p graph-algorithms --test brandes_2001 -- --nocapture > T-算法-04.json` | top10_order_equal=true AND delta_mean≤1e-4 | `{top10_ours:[], top10_brandes:[], order_equal, delta_max, sha256}` |
| T-算法-05 | A4 PageRank 公开收敛测试：d=0.85，tol=1e-8，迭代≤100，与参考 PR Δ≤1e-7 | PageRank 输出 vs 参考实现 | `cargo test -p graph-algorithms --test pagerank -- --nocapture > T-算法-05.json` | converged_iter≤100 AND delta≤1e-7 | `{damping=0.85, tol=1e-8, iter, delta, sha256}` |
| T-算法-06 | A6 RRF k=60 公开融合公式（Reciprocal Rank Fusion，Cormack 2009 标准公式） | 5 路结果融合 RRF 正确性：与 RRF 数学公式对比，Top-N 100% 一致 | `cargo test -p mox-expert --test rrf_fusion -- --nocapture > T-算法-06.json` | top20_equal=true | `{k=60, input_5_lists, rrf_ours, rrf_math, top20_equal, sha256}` |
| T-算法-07 | A8 CPM 关键路径 + RCPSP 公开基准（RCPSP J30 标准集） | 在小型 J30 样例 1 上，关键路径长度 ≤ 公开解 + 0，RCPSP 调度合法 | `cargo test -p optimizer --test rcpsp_cpm -- --nocapture > T-算法-07.json` | critical_path_length=公开解 AND schedule_valid=true | `{critical_path_length_ours, critical_path_length_ref, delta, schedule_valid, resource_conflicts, sha256}` |
| T-算法-08 | 关图噪声清洗 Aura-Alg-05（21 §五 算法 5 + 22 表 6 Aura-Alg-05 = A1 社区 + A6 RRF 组合） | 噪声注入 10 条重复边，Aura-Alg-05 清洗后，去除冗余边率 ≥ 90% 且 Δ 关图主指标 ≤ 1% | `cargo run -p info-graph -- denoise --sample docs/graph/graph.enterprise.json --inject-noise 10 --json > T-算法-08.json` | removed_noise_ratio≥0.9 AND graph_delta_pct≤0.01 | `{noise_injected=10, noise_removed, removed_ratio, graph_size_delta_pct, sha256}` |

### 第三大类 · T-安全（RBAC + 审计链 6 题 = T0-05/06 + 4 扩展，共 6 题）
| 题号 | 来源/SoT | 描述 | 命令 | 阈值 | JSON |
|------|----------|------|------|------|------|
| T-安全-01（T0-05） | 12 §4 Probe-01~11 SoT / NIST RBAC 标准 | 严格模式 11 探针 11/11 | `cargo run -p governance-console -- --probe --all 11 --json > T-安全-01.json` | 11/11 PASS | `{probes:[{id,name,result,detail}], pass_count=11, total=11, sha256}` |
| T-安全-02（T0-06） | 12 §6 6 字段可查询 + SHA-256 哈希验证 SoT | 插入 1 条 → 查询 6 字段 + hash 复算一致 | `cargo run -p governance-console -- --audit smoke --subject S_SMOKE_$$ --json > T-安全-02.json` | inserted=true AND queried_match=true AND hash_verify=true | `{subject, inserted_id, 6_fields_match, hash_verify, sha256}` |
| T-安全-03 | 12 §3 6 角色矩阵 兼容模式 + 严格模式双模式验证 | 6 角色 × 10 关键端点 × 2 模式 = 120 组合正确 | `cargo test -p rbac-engine --test role_matrix -- --nocapture > T-安全-03.json` | expected_120_match_pct=100% | `{roles=6, endpoints=10, modes=2, total=120, matches=120, mismatches:[], sha256}` |
| T-安全-04 | Owasp Top 10 SQL 注入 + NoSQL 注入测试公开 Payload 清单（50 条经典 Payload） | 10 个关键 API 喂 50 条 OWASP 注入 Payload，全部返回 4xx/5xx，无 200 且日志留痕 | `cargo test -p gateway-runtime --test owasp_injection -- --nocapture > T-安全-04.json` | blocked_pct=100% AND logged=100% | `{payloads=50, endpoints=10, blocked_count, logged_count, leaked_samples:[], sha256}` |
| T-安全-05 | JWT HS256 签名验证公开标准（RFC 7518） + 会话超时 30 min | 篡改 JWT 签名 = 401；过期 31 min Token = 401；合法 0~29 min = 200 | `cargo test -p rbac-engine --test jwt_rfc7518 -- --nocapture > T-安全-05.json` | all_3_cases_pass | `{jwt_tamper_status_401, jwt_expire_31m_401, jwt_valid_200, sha256}` |
| T-安全-06 | 操作日志全留存（22 表 8 NFR-02 安全 = 操作日志全留存 硬要求） | 全仓所有写操作（20 个关键写端点）执行后，audit_chain 100% 命中 | `cargo test -p audit-chain --test write_logging -- --nocapture > T-安全-06.json` | audit_hit_pct=100% | `{write_endpoints=20, logged=20, missing_ops:[], sha256}` |

### 第四大类 · T-治理（治理闸门 Verify 14 专家 + P9 判重 6 题 = T0-07/08 + 4 扩展，共 6 题）
| 题号 | 来源/SoT | 描述 | 命令 | 阈值 | JSON |
|------|----------|------|------|------|------|
| T-治理-01（T0-07） | 08 §2 Verify 14 专家 SoT（14 位专家定义 = 18 TOP-MASTER §四/六 治理定义） | 14 专家 Verify 14/14 PASS，每个 ≥ 1 节点处理 | `cargo run -p mox-expert --bin mox_optimize -- --step Verify --sample docs/graph/graph.enterprise.json --json > T-治理-01.json` | 14 PASS | `{experts:14, pass:14, warn:0, fail:0, nodes_processed_per_expert:[], sha256}` |
| T-治理-02（T0-08） | 16 §3 P9 0 新增缺陷 SoT / P9 判重算法公开余弦相似度阈值 0.95 | guantu_gate.py --strict + dedup 0 新增缺陷 + ≥0.95 禁合并 403 | `python tools/guantu_gate.py --strict --json > T-治理-02.json` + `curl -s -X POST /api/dedup/test-merge --json '{"id":SIM_096}' \| jq .code` | new_defects=0 AND merge_highsim_code=403 | `{new_defects, high_similarity_count, merge_forbidden_403=true, ratchet_trend, sha256}` |
| T-治理-03 | 08 §2 Reconcile 7 对账 SoT（7 类任务 = 7×8 对账） | Reconcile 全量对完后输出 Δ 报告 = 56 条全部颜色正确，无 RED | `cargo run -p mox-expert --bin mox_optimize -- --step Reconcile --json > T-治理-03.json` | reconcile_red=0 | `{red,orange,green, reconcile_report_url, sha256}` |
| T-治理-04 | 08 §2 Govern 治理最终裁决 SoT：Govern 对 8 步全程最终输出 = 通过/警告/失败 三态 | Govern 输入 T0~T0-18 18 题数据 = 全部 PASS → Govern 裁决 = ALLOW；改 1 题 FAIL → Govern 裁决 = DENY | `cargo run -p mox-expert --bin mox_optimize -- --step Govern --mock t0_pass=18 --json > T-治理-04a.json` + `--mock t0_pass=17` → DENY | a=ALLOW AND b=DENY | `{mock_t0_pass:18, verdict:ALLOW}; {mock_t0_pass:17, verdict:DENY}; sha256a+sha256b` |
| T-治理-05 | 四闸门（06 §四 G1 需求/G2 架构/G3 实现/G4 治理）每闸门 10 项子项 × 4 = 40 子项全 PASS | 四闸门 40 子项全 PASS | `cargo run -p governance-console -- --gates detail --json > T-治理-05.json` | gates_40_subitems_pass=40 | `{gates:[{id, subitems_pass, subitems_total}], pass_count=40, total=40, sha256}` |
| T-治理-06 | 需求覆盖率（21 §十一 99×4=396 钩 SoT）：后端自动化覆盖率 + 前端自动化覆盖率 + 测试自动化覆盖率 = 三项 ≥ 95% | 钩位自动映射到测试用例，覆盖率 396 钩全 PASS | `cargo run -p governance-console -- --coverage srs --json > T-治理-06.json` | coverage_srs_396_pct≥95，且前端≥90，后端≥95，测试≥95 | `{srs_total=396, covered, pct, fe_pct, be_pct, test_pct, missing_items:[], sha256}` |

### 第五大类 · T-AI（AI 四端点 4 题 = T0-11/12 + 2 扩展，共 4 题，严格单次+0 降级+SHA-256 匹配 99%+）
| 题号 | 来源/SoT | 描述 | 命令 | 阈值 | JSON |
|------|----------|------|------|------|------|
| T-AI-01（T0-11） | 25 §2 30 题 严格单次 公开标准题库 SoT（GSM8K×5/HumanEval×3/MMLU×5/常识×5/CMMLU×5/TODAY×2/JSON精确×5 = 7 大类） | 30 题严格单次 0 重试 0 本地降级 30/30 100%，P95≤35761ms，SHA-256 匹配≥99% | `cd backend-node && node test/ai-engine-real-benchmark.js --strict-single --no-retry --json > T-AI-01.json` | strict=30/30 degraded=0 p95≤35761 sha_match≥99% | `{summary:{strict, degraded, p95_ms}, per_question_30:[{id,category,pass,sha256_ans}], match_25_report_pct, sha256}` |
| T-AI-02（T0-12） | 20 §D6 AC-10 四端点 SoT / 22 表 6 算法路由规则 | `/ai/engine/process` 意图路由正确率：数学/逻辑→reasoning，其它→chat；4 端点全部返回 200 | `cargo test -p ai-engine --test endpoint_routing -- --nocapture > T-AI-02.json` | routing_correct_pct=100 AND status_code_4_endpoints_all_200 | `{routing_tests=?, correct, pct, endpoints_4:{cap,proc,ana,exp} all_200, sha256}` |
| T-AI-03 | Embedding 向量召回 R@5 ≥ 0.9（公开文档召回基准：MS MARCO Dev Small 1000 条子集） | 向量召回 R@5 ≥ 90%，且走 A6 RRF k=60 与关键词混合召回后 R@10 ≥ 95% | `cd backend-node && node test/ai-engine-embedding-recall.js --strict > T-AI-03.json` | recall_at_5≥0.9 AND rrf_recall_at_10≥0.95 | `{r_at_5, r_at_10_rrf, queries=1000, top_failures:[], sha256}` |
| T-AI-04 | 幻觉检测（21 §一.2/§二.1 明确写：符号图谱为唯一真相源，大模型幻觉不得写入确定性图谱） | 故意喂 10 条「虚构需求 REQ-9999」的大模型建议 → 人工审核页标记为幻觉 → 写入关图的成功率 = 0（绝对不能写入） | `cargo test -p governance-console --test hallucination_guard -- --nocapture > T-AI-04.json` | hallucinated_writes_blocked_pct=100 | `{hallucination_tests=10, blocked=10, leaked_to_graph=0, sha256}` |

### 第六大类 · T-前端（前端质量与 P1 人人爱用 5 题 = T0-13/14 + 3 扩展，共 5 题，必须真实跑，不能用 MSW Mock 当企业级验收）
| 题号 | 来源/SoT | 描述 | 命令 | 阈值 | JSON |
|------|----------|------|------|------|------|
| T-前端-01（T0-13） | 26 §F7 Lighthouse ≥90×3 公开标准（Google Lighthouse 官方评分） | FusionView 首页三项≥90；12 看板随机抽 3 个（K01 8 步/K04 56 热力图/K06 11 探针）每项三项≥85 | `cd frontend-ui && ./node_modules/.bin/lighthouse-batch -s http://localhost:4173/,http://localhost:4173/#/k01,http://localhost:4173/#/k04,http://localhost:4173/#/k06 --out T-前端-01/` | FusionView P/A/BP≥90；K 页 ≥85 | `{pages:[{url,p,a,bp}], overall_min=90, kiosk_min=85, sha256}` |
| T-前端-02（T0-14） | 26 §F7 Playwright ≥ 15 P0 E2E 关键路径（登录/8步/P9/对账/命名切换/回滚/严格模式/推送 等 15 条） | Chromium+Firefox 双浏览器 P0 15/15 全 PASS | `cd frontend-ui && npx playwright test --grep P0-CRITICAL --browser=chromium --browser=firefox --reporter=json > T-前端-02.json` | expected≥15, unexpected=0, flaky=0 | `{browsers=2, stats:{expected,unexpected,flaky,skipped}, per_case:[], sha256}` |
| T-前端-03 | 26 §F7 Vitest 覆盖率 ≥ 85%（前端自验 All-04 延续到企业级验收真实代码） | Vitest 行覆盖率 ≥ 85%，分支 ≥ 80% | `cd frontend-ui && npm run coverage -- --reporter=json-summary --outputFile T-前端-03.json 2>/dev/null ; cat T-前端-03.json \| jq ".total.lines.pct, .total.branches.pct"` | lines≥85，branches≥80 | `{lines_pct, branches_pct, funcs_pct, stmts_pct, sha256}` |
| T-前端-04 | 26 §2 铁律 13 响应式 + 无障碍 A11y（WCAG AA 对比度 + 键盘 Tab 全可达） | 1280/1920/3840 + 375 四尺寸 Playwright 视觉回归 ≤ 0.5% 像素差异；键盘 Tab 全可达 10 个关键页面无死循环 | `cd frontend-ui && npx playwright test --grep VISUAL_REGRESSION --reporter=json > T-前端-04a.json` + `npx playwright test --grep KEYBOARD_A11Y > T-前端-04b.json` | visual_diff_pct≤0.5（4 尺寸）AND keyboard_tour_count=10 all_pass | `{visual:[{size,diff_pct}], keyboard:{pages=10,pass=10}, sha256}` |
| T-前端-05 | 26 §F1 4 全局组件（G01 命名切换/G02 RBAC 双模式/G03 通知中心/G04 Shepherd 新手引导） | Playwright 模拟：命名切换后 20 处文本 100% 变；严格模式探针 10/11 时所有写按钮 disabled；通知中心红点点击能跳转；Shepherd 10 步走完 | `cd frontend-ui && npx playwright test --grep "GLOBAL_COMPONENT" --reporter=json > T-前端-05.json` | all_4_components_pass AND naming_switch_text_changed_pct=100 AND strict_mode_disables_writes=true AND shepherd_10_steps_finish=true | `{g01:pass, g02:pass, g03:pass, g04:pass, details, sha256}` |

### 第七大类 · T-集成（全链路端到端 + 对外 SRS 功能 5 题 = T0-15/16 + 3 扩展，共 5 题，必须走真实 8 步，不得跳步/不得 Mock）
| 题号 | 来源/SoT | 描述 | 命令 | 阈值 | JSON |
|------|----------|------|------|------|------|
| T-集成-01（T0-15） | 08 mox_optimize 8 步 SoT + 22 表 3 6 阶段 × 8 步 | 真实 sample graph 走完全 8 步，8 状态 PASS/8，节点数变更与预期 ≤±5% | `cargo run -p mox-expert --bin mox_optimize -- --sample docs/graph/graph.enterprise.json --strict --all 8 --json > T-集成-01.json` | step_8_pass_all=8/8 AND node_delta_pct≤5 | `{steps_8:[{name,status,nodes_processed,time_ms}], total_time_ms, final_nodes, sha256}` |
| T-集成-02（T0-16） | 06 §四闸门 G1/G2/G3/G4 SoT | 端到端跑完后，四闸门 4/4 PASS | `cargo run -p governance-console -- --gates audit --sample out/e2e-sample-graph.json --json > T-集成-02.json` | gates_pass=4/4 | `{gates:{g1:pass,g2:pass,g3:pass,g4:pass}, sha256}` |
| T-集成-03 | 21 §三 3.5 工程代码映射与绑定（R6/R7 绑定率 97.9% SoT） | 代码绑定率 = 模块→文件→函数 精准关联 97.9%+，未绑定清单 ≤ 50 条 | `cargo run -p code-binder -- --scan workspace --bind --json > T-集成-03.json` | bind_rate≥97.9 AND orphans≤50 | `{bind_rate_pct, orphans_count, by_layer:{qi,ti,shen,ji}, sha256}` |
| T-集成-04 | 21 §三 3.1 需求接入抽取 + 3.2 业务流程建模 真实非结构化 Markdown 10 份抽取准确率 ≥ 90% | 10 份真实 SRS Markdown 喂 Aura-Alg-01 文档解析算法，抽取 200 条需求实体，与人工标注 SoT 对比 F1≥0.9 | `cd backend-node && node test/doc-parse-benchmark.js --strict --json > T-集成-04.json` | f1_score≥0.9 | `{docs=10, entities_extracted=200, precision, recall, f1, sha256}` |
| T-集成-05 | 21 §五 5.4 变更影响推演高阶能力（企业版神道 V2.0 = 激活扩散 A5 + 拓扑排序 A8 CPM） | 模拟变更 REQ-0142 节点 → 推演链路命中节点数 ≥ 人工金标 90%，工作量评估偏差 ≤ 20% | `cargo run -p info-graph -- whatif --change REQ-0142 --json > T-集成-05.json` | affected_match_pct≥90 AND effort_error_pct≤20 | `{gold_affected, our_affected, match_pct, gold_effort_h, our_effort_h, error_pct, sha256}` |

### 第八大类 · T-NFR 与版本里程碑（对外 4 版本 + 5 NFR 硬门槛 6 题 = T0-17/18 + 4 扩展，共 6 题，全部来自 22 表 7 + 表 8 = 写死阈值不是你自己拍）
| 题号 | 来源/SoT | 描述 | 命令 | 阈值 | JSON |
|------|----------|------|------|------|------|
| T-NFR-01（T0-17） | 22 表 7 V1.0 气道里程碑 M0 = L0/L1/L2 三级门槛 SoT | M0 气道三级验收全过 | `cargo run -p governance-console -- --milestone M0 --check --json > T-NFR-01.json` | m0.l0_pass AND m0.l1_pass AND m0.l2_pass | `{m0:{l0,l1,l2}, all_pass=true, sha256}` |
| T-NFR-02（T0-18） | 22 表 8 NFR-01 性能 p50 ≤ 200ms SoT + p99 ≤ 1000ms（写死 2 个阈值） | 1000 请求 50 并发 p50 ≤200ms，p99 ≤ 1s | `ab -n 1000 -c 50 -k -g T-NFR-02.tsv http://localhost:3000/api/health 2>&1 > T-NFR-02.txt ; awk 'END{print p50,p99}'` | p50≤200, p99≤1000 | `{p50_ms, p99_ms, rps, failed_requests=0, sha256}` |
| T-NFR-03 | 22 表 8 NFR-02 安全（RBAC 11 探针 + 审计链 + OWASP，= T-安全-01/02/04 聚合）+ 企业私有化多租户数据隔离 3 租户互不可见 | 多租户隔离：租户 A/B/C 各造 100 条数据，跨租户查询返回 0 条 | `cargo test -p rbac-engine --test multi_tenant -- --nocapture > T-NFR-03.json` + `jq` 汇总前三项安全 | cross_tenant_leaks=0 AND safe_3_pct=100 | `{multi_tenant:{tenants=3,leaks=0}, security_3_agg:{rbac11_pass, audit_6_pass, owasp_block_pct=100}, sha256}` |
| T-NFR-04 | 22 表 8 NFR-03 可扩展：图谱 Schema 新增 1 个节点族 + 1 个边族 + 重启 ≤ 10s + 历史数据 0 丢 | 热扩 + 重启 ≤ 10s + 数据完整 | `cargo run -p graph-schema-tool --bin evolve_schema -- --add-family EXT_TYPE --add-edge EXT_EDGE --json > T-NFR-04.json` | restart_ms≤10000 AND data_loss_pct=0 | `{schema_evolve:{new_node, new_edge}, restart_ms, before_nodes, after_nodes, loss_pct=0, sha256}` |
| T-NFR-05 | 22 表 8 NFR-04 兼容性：Git 工程 + Rust AIS 目录 + 云盘 WebDAV 适配 3 种外部系统 100% 对接成功 | 3 对接测试通过 | `cargo test -p doc-hub --test external_integration -- --nocapture > T-NFR-05.json` | git_ok AND rust_ais_ok AND webdav_ok = all_3_pass | `{git:true, rust_ais:true, webdav:true, pct=100, sha256}` |
| T-NFR-06 | 22 表 8 NFR-05 易用性（15 P1 人人爱用 + P2 全自动）：新成员 onboarding 10 人真实测试 ≤ 2 小时完成 80% 关键任务；8 步一键按钮完成时间中位数 ≤ 15 分钟 | 真实 Playwright 脚本模拟 10 新成员 onboarding 完成率 ≥ 80%，时长 ≤2h；一键 8 步 ≤ 15min 中位数 | `cd frontend-ui && npx playwright test --grep ONBOARDING_10_USERS --reporter=json > T-NFR-06a.json` + `...8_STEP_ONECLICK... > T-NFR-06b.json` | onboarding_80pct_2h AND oneclick8_median_p50≤900s | `{onboarding:{users=10, completion_pct≥80, median_s≤7200}, oneclick8:{median_s≤900}, sha256}` |

> **题库汇总**：T-工程 8 + T-算法 8 + T-安全 6 + T-治理 6 + T-AI 4 + T-前端 5 + T-集成 5 + T-NFR 6 = **合计 48 题** + T0 烟测 18 题（作为硬门槛，与大类重复部分已包含在 48 题中，不重复计数，只作为**过门槛**必须先做）；实际 SoT 总数 **48 题 ≥ 50 的目标（写 48 是精确数，已经 ≥ 40+ 的企业级标准=够了）**，T0=18 烟测先行，T0 全过再跑 48 题，**最终完整报告运行题数 = 18 + 48 = 66 次执行**（T0 18 和 48 中重复的 10 题，你只跑一次，结果复用，标注 reused=true 避免浪费时间）。

## 九、📦 T1~T7 你必须交付的 7 份测试交付物（按顺序，每份有独立门槛，缺 1 = 整体不通过 + 总设计师不签字）
> 所有交付物文件统一存：`docs/enterprise/_data/test-report-YYYYMMDD-HHmmss-<T编号>/` 目录（永久留痕 + SHA-256 索引）。

### T1 · 《测试计划（含里程碑分组 + RACI + 8 大类 48 题执行时间排期 + 四闸门 L2 放行策略）》
- 格式：4 表格：① 测试范围（8 大类 48 题清单 + 对应 SoT 锚点 + PASS 阈值抄录）；② 执行排期（按 M0~M8 9 里程碑 × L0/L1/L2 三级，每题分到哪一级验收）；③ RACI（测试 A/开发 C/算法 C/产品 C/总设计师 I 对应每题谁签字）；④ 风险与回滚计划（FAIL 了怎么回滚到上一个已知好版本）。
- 门槛：① 48 题每题的 SoT 锚点必须写「上游文档名+行号」（不能写「忘了从哪来的」）；② M0~M2 气道/体道里程碑必须至少分配 28 题；M3~M8 神道/技道里程碑分配剩余 20 题（V2.0 神道 V3.0 技道 4 版本里程碑全覆盖 22 表 7）。

### T2 · 《测试用例脚本 & meta-test 自验报告》（= 所有 48 题脚本 + 每个大类至少 1 个 meta-test 证明你脚本确实能报 FAIL）
- 格式：① 48 个脚本统一路径登记（`tests/enterprise/T-大类-xx.{rs,sh,py,ts,spec.ts}`）+ 元数据 JSON 索引；② 8 大类 meta-test 报告（每类至少 1 个，比如 7×8 对账你故意插入 Δ=1e-4 的坏数据 → 看脚本是否报出 1 RED + FAIL，证明你不永远绿；或前端 Playwright 故意隐藏按钮 → 报 FAIL；或 AI 插入超时 → 报 FAIL）；③ 每个 meta-test 输出 FAIL 截图/JSON，证明脚本判 FAIL 逻辑正确（骗分检测硬门槛）。
- 门槛：① 8 大类每个 ≥ 1 个 meta-test 自验 100% 能正确识别 FAIL；② 48 脚本都能「1 行命令」复现，任何人都能跑（= 写清楚环境准备：Rust toolchain / Node / Python 版本 / 数据目录）。

### T3 · 《全维自动化测试流水线配置（CI/CD YAML）+ P2 一键全自动运行》
- 格式：① `.github/workflows/enterprise-test-suite.yml` 或 GitLab CI `.gitlab-ci.yml`：**一键触发 = 自动跑 T0 18 烟测 → T0 PASS → 并行跑 8 大类 48 题 → 自动汇总生成 T4 报告 PDF/Markdown → 自动把 JSON 存 _data 目录并 SHA-256 索引**；② Slack/飞书 Webhook：FAIL 自动@对应 RACI；③ 运行时间估算（全流程 ≤ 3 小时，48 题+T0 18 = 66 次执行并行化）。
- 门槛：① 点一下按钮 3 小时出完整报告，零人工干预（P2 全自动最高优先级）；② T0 任意一题 FAIL = 流水线立刻中止，不跑后面，节省算力（你不能 FAIL 了还浪费算力跑几十题）。

### T4 · 《企业级测试记录报告（最终交付物 = 客户/审计/对外签约看的正式报告）》
- **格式（必须按这个结构写，客户看的，P1 人人爱用 = 人人能看懂，不能全是术语堆）**：
  1. 📄 封面（版本号/日期/测试联盟 A+三联盟 C+总设计师 I 签字位）
  2. 📊 执行摘要（1 页 = 给老板看的：T0 18/18？48 题 PASS/FAIL/WARN 多少？四闸门 4/4？L2 放不放行？AI 30/30？一句话结论：✅ RELEASE PASS / ❌ REJECT FIX）
  3. 🌳 权威链对齐表（22 号 9 大表对应行，证明所有测试题命名/阈值一致）
  4. 🧪 T0 烟测 18 题结果汇总表 + 每道题 SHA-256 链接
  5. 🔬 8 大类 48 题详细结果表（8 个章节 × 每章 1 表格：题号 + 命令 + 结果数字 + PASS/FAIL + SHA-256 链接 + 对应 SoT 锚点）
  6. 🔴 FAIL 与 WARN 根因分析表（如果有 FAIL：题号 + 根因 + 责任方 + 修复建议 + 预计修复工时 + 重测计划）
  7. ⚖️ 四闸门 L2 放行判定矩阵（G1/G2/G3/G4 4×详细结果 + 证据链接）
  8. 📈 棘轮趋势对比图（和上一版 11/12/13/16/25 基线数字对比 = 15 P9 全维棘轮可视化）
  9. 🔎 公开来源声明 & 可复现指南（1 页：「任何人按附录命令可复现 99% 以上结果」声明 + 环境版本：Rust/Node/Python/系统信息）
  10. 🔒 诚信声明（测试联盟 A 签字：「我承诺本次 48 题全部真实执行，严格单次、零重试、零放水、零骗分、SHA-256 留痕齐全，如有虚假愿负全责」）
  11. 📎 附录 A：17 份必读签字页
  12. 📎 附录 B：10 条实跑命令 ≥ 22 张截图（含 5 张 Mock FAIL 截图）
  13. 📎 附录 C：所有 48 + 18 = 66 次执行 SHA-256 索引表（题号 + JSON 路径 + SHA-256 值）
  14. 📎 附录 D：8 大类 meta-test 自验报告汇总（证明不永远绿，至少 8 个 FAIL 证据）
- 门槛：① 14 节齐全 = 缺 1 节 = T4 不合格；② FAIL 必须有根因分析，不许写「不知道为啥坏了」；③ 诚信声明 + 四方签字缺一不可。

### T5 · 《变更关联表（本次测试发现问题 → 对应 21 §十一 396 钩 → 对应 22 号哪张表哪行 → 对应 05 里程碑哪一个）》
- 格式：N 行 6 列矩阵：① 问题编号（FAIL-001/…）② 归属大类 ③ 对应 21 §十一 钩位（哪一条 SRS 原子点失败？）④ 对应 22 号 9 表哪一行（名实分裂失败？）⑤ 对应 05 里程碑 Mx Lx ⑥ RACI 责任方（该谁修）。
- 门槛：① 所有 FAIL/WARN 必须 100% 能在 T5 表找到对应锚点（找到 21/22/05 三个锚点 = 证明你发现的问题是真的重要，不是小题大做）；② 至少覆盖 M0~M4 气道/体道/神道前三道里程碑 ≥ 24 行（即使无 FAIL，也要写 PASS 条目，证明你按里程碑覆盖了）。

### T6 · 《最终判定与交付签字（= 最关键一页 = 决定版本能不能发）》
- 格式：3 部分：
  1. **放行判定树（写死 7 条 = 7 条全 ✅ = 放行，否则任意 1 条 ❌ = 拒绝放行）**：
     | # | 判定条件（必须全 ✅） | 本版结果（✅/❌ + 证据 T4 页码） |
     |---|----------------------|--------------------------------|
     | ① | T0 18/18 全 PASS |  |
     | ② | 8 大类 48 题 严格 PASS ≥ 46（允许 2 WARN，0 FAIL 硬底线） |  |
     | ③ | 7×8 对账 Δ≤1e-6 56/56 全绿（0 RED = 硬底线，1 RED = 不通过） |  |
     | ④ | 棘轮 5 份（11/12/13/16/25）对比 = 0 退化（所有数字 ≥ 基线数字） |  |
     | ⑤ | 06 四闸门 G1/G2/G3/G4 4/4 全 PASS（L2 放行硬条件） |  |
     | ⑥ | 21 §十一 396 钩 测试覆盖率 ≥ 95%（对外 SRS 承诺必须几乎全覆盖） |  |
     | ⑦ | 诚信声明 + meta-test 8/8 自验通过（证明你不骗分，脚本确实能识别 FAIL） |  |
  2. **最终结论一句话（必须写，不能模糊）**：
     - ✅ **RELEASE_L2_PASS（企业级对外可签约/投标/发布 V1.0 气道版 GA）**：7 条全 ✅
     - ⚠️ **RELEASE_L1_CONDITIONAL_PASS（内部可用，外部不行 = 需修复 WARN 后再升级到 L2）**：①③④⑤⑥⑦ 全 ✅，② = 45 PASS / 3 WARN / 0 FAIL
     - ❌ **REJECT（拒绝放行，修复后重测）**：任意 1 条不满足（比如 T0 17/18，或 7×8 1 RED，或棘轮退化，或四闸门 3/4）
  3. **四方签字位**（测试联盟 A + 开发联盟 C + 算法联盟 C + 产品联盟 C + 总设计师 I 放行 = 5 人签字，缺 1 个签字 = 结论无效）
- 门槛：① 7 条判定条件结果必须写证据 T4 页码（比如 T4 §4 第 3 页），不许空打钩；② 最终结论必须 3 选 1，不许写「待讨论」「基本通过」这种模糊话。

### T7 · 《归档记录（到 09 完成归档 + 05 里程碑登记 + 22 号总控卡对应表行更新）》
- 格式：3 张表：① 归档文件清单（T1~T6 6 份交付物 + 所有 JSON + 所有 SHA-256 索引 = 统一路径永久留痕）；② 05 里程碑更新（Mx Lx 验收结果 = 写进 05 §7 里程碑表对应行）；③ 22 号总控卡对应表行更新（表 8 NFR 棘轮阈值：如果本版本有提升，比如 649 → 655 tests，那么更新表 8 棘轮基线 = 更新 22 号卡对应行，并四签字走 22 号改卡流程）。
- 门槛：① 所有 JSON 必须真实存在于 `docs/enterprise/_data/`，并加 `git commit` 登记（不能只放你电脑上）；② 22 号总控卡如果提升了棘轮数字 = 必须按 22 号改卡流程四签字（总设计师 A+产品 R+算法 R+开发 R 全签）才能改，否则你私自改 = 越权，T7 作废。

## 十、❌ 7 项禁止事项（命中任意 1 = 报告作废，所有测试数据清零，工时 0 不计，记入越权清单）
1. ❌ 禁止骗分：改题 / 改阈值 / 改 8 步名 / Mock 当真实数据 / 重试凑通过 / 永远绿没测 FAIL 场景。
2. ❌ 禁止主观：出 6 字段不全的题 / 用「我觉得 OK」/「这个界面挺好看」当 PASS 依据 = 作废。
3. ❌ 禁止棘轮下降：测的数字 < 11/12/13/16/25 基线，还写成 PASS = 作假，作废。
4. ❌ 禁止 SHA-256 不全：任一题目 JSON 没算 SHA-256 留痕 = 作废。
5. ❌ 禁止绕过四闸门：G1/G2/G3/G4 缺 1 PASS 还签 L2 放行总设计师 = 作废且你负全责。
6. ❌ 禁止名实分裂：测试题命名 ≠ 22 表 1/2/3/4/5/6/7/8 = 命名不对 = 作废。
7. ❌ 禁止甩锅：FAIL 根因不写，写「开发还没给我版本所以我只能用老的测」/「AI Key 过期所以 0/30 也写成 PASS」= 甩锅 = All-04 你自验闭环 = 你主责 = 作废。

## 十一、成功/失败判定（给总设计师/三联盟看）
### ✅ 成功（RELEASE_L2_PASS = 全部满足，缺一降一级或拒绝）
1. §1 17 份必读签字 + 10 实跑 ≥ 22 张截图（含 ≥ 5 Mock FAIL 截图）齐全；
2. T0 18/18 烟测 100% PASS；
3. 8 大类 48 题：≥ 46 PASS + ≤ 2 WARN + **0 FAIL（硬底线，有 1 FAIL = 直接降级到 REJECT）**；
4. 7×8 算法对账 56/56 100% 全绿（Δ≤1e-6 红线 = 0 RED，1 RED=REJECT）；
5. 棘轮对比 11/12/13/16/25 五份基线：0 退化（所有数字 ≥ 基线，提升允许，下降禁止）；
6. 06 四闸门 G1/G2/G3/G4 4/4 全 PASS；
7. 诚信声明 + 8 大类 meta-test 8/8 全部能正确识别 FAIL（证明脚本不永远绿=没骗分）；
8. 21 §十一 SRS 覆盖率 ≥ 95%（对外 SRS 承诺全覆盖）；
9. T1~T7 7 份交付物全部满足门槛；
10. RACI 5 签字（T6）齐全；
11. 所有 48+18=66 JSON 输出 SHA-256 100% 留痕，可复现 1 行命令 99% 匹配。
### ⚠️ 条件 PASS（L1 内部可用，L2 外部拒绝：需修复 WARN 再补测）
成功条件 ①③④⑤⑥⑦⑧⑨⑩⑪ 全部满足，② = 45 PASS / 3 WARN / 0 FAIL（最多 3 个 WARN，WARN 定义 = 阈值超 10% 以内但不影响核心功能，比如 Lighthouse 88 分 = WARN，46 题 PASS 1 WARN=仍然 L2）。
### ❌ 失败（REJECT · 拒绝放行，必须修复重测）
命中以下任一 = REJECT：① T0 17/18 或更差；② 48 题 ≥ 1 FAIL；③ 7×8 ≥ 1 RED；④ 棘轮退化 1 条；⑤ 四闸门 1 FAIL；⑥ meta-test < 8/8；⑦ SHA-256 < 66/66；⑧ 诚信声明造假；⑨ RACI 签字缺。

## 十二、你现在立刻做的 7 步（按顺序，一步都不能跳）
1. 填 27 §1 三梯队 17 份必读签字 + 0.5 行摘要卡 → 全部签字；
2. 实跑 §1 第三梯队 10 条命令（⑭~㉓），保存 ≥ 22 张截图（**重点：⑰⑱⑲㉒㉓ 这 5 条必须至少各有 1 张 Mock FAIL 截图，证明你判 FAIL 逻辑确实存在不永远绿**），6 份 JSON 基线算 SHA-256 → 这是骗分检测硬门槛，缺 1 张 Mock FAIL = 你直接不通过，后面不用做；
3. 跑 T0 烟测 18 题（18/18 PASS = 继续；否则直接出 T6=「REJECT 拒绝放行 T0 第 X 题失败」，不要浪费时间跑 48 题）；
4. T1 测试计划 → T2 48 脚本 + 8 meta-test 自验（每个大类 1 个 FAIL 证据）→ T3 CI YAML 1 键全自动；
5. 一键触发 CI YAML → 3 小时内出 48 题结果 JSON 全集 → 生成 T4 报告 14 节齐全 + SHA-256 索引 ；
6. 生成 T5 锚点表 → T6 7 条判定 3 选 1 最终结论（RELEASE_L2 / L1 条件 / REJECT）+ RACI 5 签字；
7. T7 归档：把 T1~T6 所有文件 + JSON 提交 `docs/enterprise/_data/` git commit → 更新 05 里程碑 → 如果有棘轮提升，走 22 改卡流程四签字更新 22 号 9 表对应行（表 8 棘轮基线等）。
──────────────────────────────────────────────────────────────────────────────
> （提示词结束 = ────────────── 整块可复制驱动测试专家 / 测试 AI Agent）

---

## §4 · 测试越权判定清单 10 条（给总设计师/三联盟签署方打回用，命中任一条 = 测试计划 / 脚本 / 报告 直接作废，不讨论）
> 用法：测试专家提交 T1~T7 + 报告，你按这 10 条扫；命中 ≥ 1 = 直接打回 + 记入越权清单 + All-04 惩罚（工时 0 不计 + 重做）。
1. **三单一零破防**：缺 SoT / 缺公开来源 / 缺 PASS 阈值 / 缺 1 行复现命令 / 缺 JSON / 缺 SHA-256，6 字段缺 ≥ 1 字段（= 题不合格，你自己拍脑袋定通过标准 = 作废）。
2. **严格单次破防**：AI 30 题用了 `--retry=3` / 多跑几次选一次最高成绩当结果 = 骗分 = 作废。
3. **Mock 当真实**：企业级 L2 验收用 MSW/本地 Mock 数据代替真实 Rust/Node/AI 运行结果 = 造假 = 作废。
4. **永远绿**：§1 要求 Mock FAIL 截图 < 5 张 / T2 meta-test 8 大类 < 8 个能正确识别 FAIL 的证据 = 你脚本永远输出 PASS = 你测了个寂寞 = 作废。
5. **棘轮下降**：你测 clippy warn=2/ dead_code=10 / UT 640 / 7×8=1 RED / 11 探针=10 / P9=1 / AI=27 / Lighthouse=87 → 任何一条数字低于 11/12/13/16/25 基线数字 = 质量退化你还写成 PASS = 放水 = 作废。
6. **Δ 放水**：7×8 对账 Δ=1e-5 你还标绿色 = Δ≤1e-6 破防 = 算法对账核心 = 作废。
7. **四闸门绕路**：06 G1/G2/G3/G4 有 1 FAIL，你 T6 还签 RELEASE_L2_PASS = 绕过 L2 放行门槛 = 你负全责 = 作废。
8. **SHA-256 留痕 < 66/66**：48+T018=66 次执行有 ≥ 1 次没算 SHA-256 写索引 = 不可审计 = 作废。
9. **命名不对 / 名实分裂**：你测试题里把「Verify」叫「第 5 步校验」、把「气道版 V1.0」叫「基础版」、把「E1 需求实体」叫「需求节点」= 和 22 表 1/2/3/4/5/6/7/8 不一致 = 名实分裂 = PR 阻断 / 报告作废。
10. **RACI 签字缺 / 诚信声明没签**：T6 最终判定 5 签字缺 ≥ 1 人 / 诚信声明测试联盟 A 没签 = 报告不具备对外效力 = 作废。

---

## §5 · 27 号测试主控 ↔ 11/12/13/16/20/22/25/26 上游权威链 1:1 挂接总表（保证你的测试和上游一致，冲突=改你，不得改上游）
| 27 号测试主控哪一块 | ↔ 对应上游文档的具体位置（必须一致） | 冲突时的裁决流程（= 翻 22 号 8 步裁决） |
|---------------------|----------------------------------|----------------------------------------|
| §2 铁律 4 棘轮不下降 / T-NFR 棘轮阈值对比 | ↔ `11/12/13/16` 四份棘轮验收报告的基线数字（clippy 0/649+ / 11 探针 / dead_code≤8 / P9 0） | 22 Step1 定位「棘轮数字」→ Step3 锚点 = 11/12/13/16 数字 → 你数字低于 = FAIL，不得改上游。 |
| T-算法 8 题 7×8=56 Δ≤1e-6 / 14×19 关图 / 5 算法组合 | ↔ `22` 表 4 / 表 5 / 表 6 / 18 §五 A1~A8 / 17 算子归一化蓝图 | 22 Step1 定位「算法/关图命名与阈值」→ Step3 锚点 = 22 表 4/5/6 → 不一致=改你测试题/对账脚本，不得改 22。 |
| T-安全 6 题 RBAC 探针 11 / 审计链 6 / JWT RFC 7518 | ↔ `12` 号 RBAC 验收报告 §3/§4/§6 + 22 表 8 NFR-02 安全 | 22 Step1 定位「安全阈值」→ Step3 锚点 = 12 → 低于=FAIL，不得改 12 基线。 |
| T-治理 6 题 14 专家 / P9 / Reconcile / Govern / 四闸门 | ↔ `08` §2 8 步（Verify/Reconcile/Govern）+ `16` P9 闸门 + `06` §四闸门 | 22 Step1 定位「8 步名/闸门 PASS 标准」→ Step3 锚点 = 08+16+06 → 14 专家数不得改 14→13，四闸门不能合并成 3 闸门。 |
| T-AI 4 题 30 题严格单次 / 端点路由 / 幻觉防护 | ↔ **`25` 号 AI 真实基准报告 7 大类 30 题 + 20 §二 铁律 3 AC-10 四端点** + 21 §二.1（符号图谱为唯一真相源，禁止幻觉写入） | 22 Step1 定位「AI 题库 / 端点命名 / 幻觉规则」→ Step3 锚点 = 25 + 20 D6 → 你必须严格用 25 同款脚本 30 题，不得换题库；四端点命名必须和 20 D6 一致。 |
| T-前端 5 题 Playwright + Lighthouse + 4 全局组件 | ↔ **`26` 号前端主控 F7 自验 6 条（Coverage≥85 / E2E≥15 / Lighthouse≥90 / Storybook≥150 / MSW 录屏）** + §2 铁律 5（双模式）/ §三 F1（6 主视图 + 12 看板 + 4 全局组件） | 22 Step1 定位「前端阈值/组件名」→ Step3 锚点 = 26 → 阈值必须 ≥ 26 F7（≥90 不允许降到 85）；4 全局组件必须 4/4 存在。 |
| T-工程 8 题 Clippy / UT 649+ / fmt / 15 Crate / 7 字段 Cargo / 98% 覆盖 / 真并行 | ↔ `20` 号后端主控 §2 铁律（All-04 / 四象限 / D7 CI 四闸门）+ `02` §3.2 15 Crate + 22 表 2 六层 L2 Crate 归属 | 22 Step1 定位「工程阈值/15 Crate 数」→ Step3 锚点 = 20 D7 + 02 §3.2 → 不得把 15 Crate 改成 14，覆盖率 ≥98% 不能改成 95%。 |
| T-集成 5 题 8 步 E2E / 代码绑定率 97.9% / 变更推演精度 | ↔ `08` mox_optimize 8 步（步名写死）+ `21` §三.3.5 绑定率 + §五.5.4 变更推演 | 22 Step1 定位「8 步名 / 绑定率 / 对外算法精度」→ Step3 锚点 = 08+21 → 绑定率必须 ≥97.9，不得写成 95。 |
| T-NFR 6 题 4 版本里程碑 + 5 NFR 硬阈值 + 4 闸门 | ↔ `22` 表 7（V1.0→V3.0 × M0~M8 打包门槛）+ `22` 表 8（性能 200ms / 安全 / 可扩展 / 兼容 / 易用性 2h onboarding） + `05` §7 里程碑表 | 22 Step1 定位「版本里程碑 / NFR 阈值」→ Step3 锚点 = 22 表 7 + 表 8 → 阈值必须写死，V1.0 气道门槛 = M0，不得改 M1；p50≤200ms 不能改 500ms。 |
| FAIL/WARN 根因分类 + 对外 SRS 覆盖率 + T5 关联表 | ↔ `21` §十一 396 钩 100% 清单 + `05` 里程碑 9 × 3 级 + `06` RACI 18 行 | 22 Step1 定位「SRS 验收/里程碑责任方」→ Step3 锚点 = 21 + 05 + 06 → 每个 FAIL 必须能在 21 §十一找到对应钩位、在 05 找到对应里程碑、在 06 找到对应 A 责任方。 |
| 任何测试争议（你和开发/算法/产品就 PASS 标准吵架 / 你说通过他说没通过） | ↔ 22 号末尾「8 步裁决流程」= 终极唯一入口 | 任何 3 人以上分歧 = 先查 22 号 9 大表对应行 + 按 8 步裁决流程走；10 分钟裁决，不开 3 小时会。裁决结果 = 对测试方不利 = 你必须按裁决结果修改测试题 / 改 PASS 判定，不得坚持原报告。 |

---

## §6 · 变更记录（DOC-TEST-MASTER-V1.0，每改题库 / 阈值 / 流程 / 判定标准 = 必须追加一行，不能空话）
| 版本 | 日期 | 变更内容（必须写 T-xx 哪题改了阈值 / 哪类新增了几题 / 22 哪张表同步更新了哪行 / 四签字） | 5 位签字（测试联盟 A · 开发 C · 算法 C · 产品 C · 总设计师 I 放行） |
|------|------|-----------------------------------------------------------------------------|---------------------------------------------------------|
| v1.0 | 2026-08-24 | 首发：§0 定位+5 签字位 / §1 17 份必读 + 10 实跑命令 ≥22 张截图（含 ≥5 Mock FAIL 骗分检测硬门槛）/ §2 铁律 14 条（三单一零/严格单次/Δ≤1e-6/棘轮不下降/禁骗分/前端后端自验/AI举证/四闸门/公开规范6字段/SHA-256留痕/1行可复现/22命名一致）/ §3 主控提示词（T0 18 烟测硬门槛·8 大类≥48 规范标准题库·T1~T7 7 份交付物·7 禁止·成功失败3级·立刻7步）/ §4 越权10条 / §5 27↔11/12/13/16/20/22/25/26 挂接 11 条表（归一化焊死）。题库=T-工程8/T-算法8/T-安全6/T-治理6/T-AI4/T-前端5/T-集成5/T-NFR6=48题+T0 18=66次执行；T6 7条放行判定树+RELEASE_L2/L1条件/REJECT 3选1。 | 测试____ · 开发____ · 算法____ · 产品____ · 总设计师____ |

---

> **最后一句话（贴在所有 CI YAML 顶部 + 前端 README / 后端 Cargo README 测试章节最顶部）**：
> 你出测试报告 = 你默认读过本 27 号文档，并且承诺 §1 17 份全签 + 10 实跑 ≥ 22 截图（含 ≥ 5 Mock FAIL 骗分检测）+ T0 18/18 全过 + T1~T7 7 份交付物齐全 + 三单一零 48 题 6 字段齐全 + SHA-256 留痕 66/66 + 越权 10 条 0 命中。缺一样 = 你的测试报告我不认，总设计师不签字。
