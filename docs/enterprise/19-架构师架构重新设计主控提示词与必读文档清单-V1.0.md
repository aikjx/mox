# 璇玑 RelGraph · 架构师·架构重新设计主控提示词 & 必读文档清单（Architect Master Prompt · V1.0）

> **文档身份**：架构师入职 / 架构重构的**唯一入口文档**（Master Prompt + Reading Path + 不可动边界声明）。交给架构师或驱动架构类 AI Agent 的第一份文件。
> **标准编号**：ENT-ARCH-MASTER-PROMPT-V1.0
> **版本**：v1.0 (ENT) · 最后更新 **2026-08-23**
> **权威链**：🟢 L0 第一级 → [`18-全域顶层总设计-三联盟模式-V1.0.md`](18-全域顶层总设计-三联盟模式-V1.0.md)（TOP-MASTER）。本文为 L1 第二级（架构师工作指导），所有架构设计结论不得与 18 冲突。
> **主责联盟**：算法联盟（架构图 / 算法 / 图谱正确性）+ 开发联盟（工程落地与兼容性） · 产品联盟 C（产品边界不被破坏）。
> **使用方法**：把「§3 架构重新设计主控提示词（可直接粘贴给架构师/AI）」完整复制，作为「工作起始提示词」；再按「§2 必读文档顺序」要求架构师依次阅读，通过后才允许做架构设计。禁止跳过本文直接动手改架构。

---

## 1 · 文档定位一句话（给架构师的第一句话）

> 你不是在「新建一套架构」——你是在「璇玑 RelGraph 已有 TOP-MASTER（18）的 12 章总设计、八大算法家族硬约束、三联盟铁律、9 里程碑三级验收、五年兼容承诺这一整套刚性框架内，**最优地微调模块边界、数据流、接口契约、依赖顺序、调度策略**，让 18 的每个结论更高效、更可落地、更稳健，而**绝不允许替换框架**（All-04 铁律：你作为架构/算法联盟方，必须自证自验，禁止让开发联盟为你的越权设计擦屁股）。

---

## 2 · 架构师必读文档顺序（**必须按顺序阅读并在每一份文档末尾签字确认「已读完并理解所有硬约束」**）

> 三联盟铁律 All-04：架构/算法联盟的交付 = 架构/算法联盟的验收。你**必须**为下列每一份文档产出「阅读签字与硬约束摘要页」，缺一份签字 = 不允许开始设计。阅读顺序严格由 ① → ⑯，不得跳读。

### 2.1 第一梯队：刚性法律（你绝对**不可动**的东西 · 读完共 ≈ 6 小时）

| 序号 | 文档路径（相对仓库根） | 读它要掌握什么（读完必须能默写出来的硬约束） | 阅读完必须提交的交付物 |
|---|---|---|---|
| ① | [`docs/enterprise/18-全域顶层总设计-三联盟模式-V1.0.md`](./18-全域顶层总设计-三联盟模式-V1.0.md) | 12 章全文；特别是 **§二 六层金字塔 & 2.2 层级铁律（禁止越层 / 禁止侧链）· §三 八层图谱 L0~L7 14 节点族 19 边族 & 构建校验 5 条规则 · §四 BP-01~10 全部 6 字段 · §五 八大算法家族全部参数 · §七.2 标准目录树 · §八 M0~M8 里程碑与 L0/L1/L2 门槛 · §十一 五年兼容承诺 · §十二 越权变更申请流程（12 条 ADR 申请）**。18 是 L0 最高规范总纲，任何设计改动必须先在这里找到依据。 | 「18 TOP-MASTER 硬约束备忘录」1 页：列出 12 章每章至少 3 条你认为不可动的硬约束，共 ≥ 36 条。签字=**我承诺不会在后续任何设计中绕过这些硬约束，除非我完整走完 18 §十二 12 条 ADR 申请流程并取得三联盟签署 + 总设计师双签字**。 |
| ② | [`docs/enterprise/01-requirements.md`](./01-requirements.md) §9 | **§9 ADR-DOC-001~012 全部 12 项决策**（权威链 / 三联盟 / 六层金字塔 / 八层图谱 / 路径归一 / 10BP / 八大算法 / 9 里程碑 / 统一 AI 入口 / 四归三连 / 命名 / ADR 治理本身）。这 12 条是「TOP-MASTER 级的具体治理条文」，**任何架构改动若违背其中 1 条，PR 会被 CI doc-lint 直接 fail**。 | 「ADR 对照表」：所有 12 条 ADR 的决策内容与影响面列出来。你后续做的任何架构变更必须在「影响面」一行显式标注「触碰到 ADR-DOC-xxx」，并通过合规路径登记。 |
| ③ | [`docs/enterprise/07-全维需求明确书.md`](./07-全维需求明确书.md) §3 + §三联盟四条铁规 | **四闸门 G0~G3 的语义；双收口铁律；All-01~All-04 三联盟四条铁律**（开口/量尺/出手；先判重后立项 Match Score ≥ 0.85；四归三连全程不脱钩 PR 阻断；联盟交付 = 联盟验收）。 | 「三联盟对我的 RACI」1 页：把 All-01~All-04 翻译成你作为架构师（算法联盟 R）的具体动作清单——**你需要对开发联盟交付什么、你需要对产品联盟签署什么、你被哪个闸门一票否决**。 |
| ④ | [`docs/enterprise/04-business-processing.md`](./04-business-processing.md) §3 十大标准 BP | **BP-01~BP-10 全部 6 字段**（编号/主责联盟/前置/核心步骤/闸门规则/审计与产物）；重点读 **BP-9 判重闸门与 BP-10 文档同步治理**，它们是你以后「改一个模块必须同步 06 映射 / 04 BP / 02 架构」的规范依据。 | 「业务流程 × 架构模块映射矩阵（自填）」：在 04 的 10 条 BP 每一条后面补 1 列「对应哪些 02 §3.2 的 crate」，如果你做模块拆分，要保证这个矩阵每一行都仍能匹配。 |
| ⑤ | [`docs/enterprise/05-iteration-roadmap.md`](./05-iteration-roadmap.md) §4 DoD + §7 9 里程碑 | **L0 单元 & 静态（clippy 0 / cargo test 全绿 / 前端构建 0 错 / 文档不漂移）· L1 集成 & E2E（10 BP 各 1 条冒烟 + RBAC 11 探针 + 5 E2E）· L2 SLO 三档验收门槛**；**M0~M8 九里程碑的主责联盟与核心交付物**。你做的任何架构重设计，**必须能回写为某一个里程碑的某一级验收通过的证据**——不能说「我这样架构更好」但过不了里程碑。 | 「里程碑 × 架构模块交付清单」：把 M0 当前阶段你要做的架构变更，落到 05 §7 的 M0 L0/L1/L2 三栏中。如果你做的变更不在任何里程碑的任何一级验收里，你要先申请 ADR 追加里程碑（走 18 §十二 流程），否则禁止实施。 |
| ⑥ | [`docs/GLOSSARY.md`](../GLOSSARY.md) 全文 | **7 大新术语（璇玑 RelGraph / 三联盟 / 六层金字塔 / 八层图谱 L0~L7 / 四归三连 / 9 里程碑 / 八大算法家族 A1~A8）**；**§5 命名与大小写约定 & 代码路径铁律（`platform/services/` 替代 `crates/`，`frontend-ui/` 替代 `frontend/`/`frontend-admin-ui/`，backend-node→edge-node 规划）**。你以后任何文档输出和代码路径引用必须严格按 §5 写，否则 All-03 判为「路径漂移」。 | 「术语自检表」：把 §5 中所有「固定写法」列出来，你后续输出的任何架构文档 / Mermaid 图 / 表格，必须 100% 符合。提交时附一份 diff 报告：`rg -n "(错误写法一)|(错误写法二)" 你的文档` 命中数必须 = 0。 |

### 2.2 第二梯队：当前架构事实（你改之前必须 100% 掌握现在长成什么样 · 读完共 ≈ 4 小时）

> 先摸清现在的事实，才谈"优化"。All-02 铁律——先判重后立项，放在架构上就是：先判「现在有没有这个模块/这个接口/这个能力」，判重通过（Match Score ≥ 0.85 复用）就不能再造新的。

| 序号 | 文档路径 | 读它要掌握什么 | 交付物 |
|---|---|---|---|
| ⑦ | [`docs/enterprise/02-architecture.md`](./02-architecture.md) 全文 | **§0.1 七视图 ↔ 六层金字塔 双向锚点表**；**§0.2 八大算法家族 A1~A8 的代码落点**；**§3.2 Rust Workspace 15 Crate 全表（序号 1~15 · 路径 · AIS 分层 · 核心职责 · 顶层入口 codePath · 引擎节点 id · Owner 项目）**（这是 15 crate 的唯一事实表）；**§7.4 存储后端配置唯一权威矩阵**；**§8 ADR 表**。 | 「15 crate × 六维绑定自查表」：把 §3.2 的 15 行复制到 Excel/Markdown，给每个 crate 补一列「我认为依赖了 07 四闸门的哪几道」。 |
| ⑧ | [`docs/enterprise/14-全维愿景核心架构与业务流程总纲.md`](./14-全维愿景核心架构与业务流程总纲.md) §3.2 + §3.3 + §5.2 | **核心骨架图（REQ 根 → Bind 六维 → 双璇玑十四维 → reconcile → flow-ai → ⛨璇玑 → 治理闸门 → 出码）**；**§3.3 模块表（6 组：璇玑主核 / 算子内核与执行 / 图与知识 AI / 归一化融合编排市场桥接目录 / 聚合网关前端 / 专题能力）**；**§5.2 双璇玑十四维专家矩阵（14 维度 × 优先级 × 产出类型）**。 | 「骨架链路节点清单」：把 §3.2 的骨架链每一个节点对应到 §3.3 里的一个具体 crate。 |
| ⑨ | [`docs/enterprise/06-requirements-architecture-map.md`](./06-requirements-architecture-map.md) §1 + §2 + §5 | **协作治理域 21 条 BR / 6 个 FR 族的需求→架构→模块→代码 映射**；**融合治理域 8 阶段 / 4 闸门 / 双璇玑的架构落点**；**§5 三联盟 RACI 18 行矩阵**（最重要！你做任何架构变更，都必须先在 §5 的矩阵里找到对应工作域的 R/A/C/I 三方，签字齐全才能动）。 | 「RACI 影响范围预填单」：你接下来要动的每一个 crate / 接口 / 流程，先在 §5 表中圈出哪些行受影响，填上 R/A/C/I 三方的名字，没圈的一律不动。 |
| ⑩ | [`docs/architecture.md`](../architecture.md) §0 / §1 / §2 / §19 | **OUS 父系统总架构 v7**（15 crate 还没合并成璇玑 RelGraph 之前的技术父系统架构）；**§2 OUS 五层架构**（接入层/运行时/编排/内核/数据）；**§19 璇玑内核**。这是「工程实现层面的底」——你做的任何架构抽象，最后必须能落到这个架构的具体 crate 上，否则等于空话。 | 「六层金字塔（璇玑）→ OUS 五层（父系统）对照图」：用一张 6×5 表写出每一对交叉对应的 crate，确保你不会因为"六层金字塔"说顺嘴就忘了底下其实是 15 个 Rust crate 的真实工程实现。 |
| ⑪ | [`docs/enterprise-architecture-analysis.md`](../enterprise-architecture-analysis.md) §2 + §3 | **双璇玑十四维维度模型（业务七维 7 项 / 开发七维 7 项，含 14 个具体专家文件路径）**；**§3 能力覆盖矩阵（7×n）**。你做架构重设计的目标是让此矩阵里 📋 项尽可能多的变成 ✅，而不是反过来引入新的 📋。 | 「架构优化 KPI 清单」：从 §3 矩阵中挑 ≥ 5 个 📋 项，写出你这次架构重设计具体会把哪 5 项从 📋→✅，必须给出可测断言（对应 L0/L1/L2 哪一级）。 |
| ⑫ | [`docs/enterprise/17-算子系统全维分析与归一化设计.md`](./17-算子系统全维分析与归一化设计.md)（或对应专题） | **全维度算子系统归一化现状**：operator-core / wasm / graph-algorithms / flow-ai / kg-hub / xuanji-expert 的现状图；你做架构变更**绝对不能让这个现状图的"归一度"降低**。 | 「归一度对比预声明」：先写清楚现在算子系统归一度你评估是多少分（0-100），你做完架构重设计后预计至少提升多少分，给出可量化的维度（如「跨 crate 隐式依赖数量」、「公共 trait 使用率」、「配置文件重复度」）。 |

### 2.3 第三梯队：代码级真相（你要对每一个你声称"优化"的 crate 做过代码级审阅，**至少读完关键文件** · 共 ≈ 4 小时）

> 你做的所有"架构优化"最后都要落在 Rust crate / Vue 文件 / 网关路由 / 配置矩阵里。空对空的架构图不算交付物（All-04：你自证自验，你得能跑通测试才算交付）。

| 序号 | 路径（均为真实路径，`ls` 必须能命中） | 读它要掌握什么 | 自证方式 |
|---|---|---|---|
| ⑬ | `platform/gateway/runtime/src/{routes/mod.rs, handlers/mod.rs, main.rs, ai_router.rs, rbac_middleware.rs, subservers.rs}`（Rust 聚合网关） | 所有对外路由（AI 四端点 / xuanji 优化 / 算子市场）；AC-10 路由语义；RBAC 中间件的管线顺序；subservers 15 crate 聚合方式。 | `cargo test -p runtime` 单测全绿报告截图 |
| ⑭ | `platform/services/graph-algorithms/src/lib.rs` + `scripts/reconcile_7x8.js` | **A1 CNM / A2 Brandes / A3 Harmonic / A4 PR / A5 激活扩散**的具体实现；**7×8=56 条对账 Δ≤1e-6 的对账脚本**。你做算法层架构变更，必须保证这 56 条对账全绿，否则你被算法联盟一票否决（All-04：你自验）。 | `node platform/services/graph-algorithms/scripts/reconcile_7x8.js` 运行输出。若你改动算法层，对账 Δ=0 的新报告必须附。 |
| ⑮ | `platform/services/xuanji-expert/src/{pipeline.rs, reconcile.rs, verify.rs, govern.rs, experts/{business,algorithm,permission,resource,security,data,observability,architecture,security_code,code_quality,performance,testing,documentation,maintainability}.rs}`（14 位专家 + 裁决 + 璇玑 + 治理） | **reconcile 的 DIM_PRIORITY（Permission/Security=100 最高）**；**verify 5 项阻断级检查（topology / data_dep / conflict / gains / code_rt）**；**govern 五条件 approved 公式**。 | 你变更此层必须附「阻断级检查对照表 + 前后对比 + 新 veto 案例数不增反降」报告。 |
| ⑯ | `platform/services/primiflow-fusion/src/unified.rs::full_gate` + `sixdim.rs` + `registry.rs` | **G0/G1/G3 全 8 项治理闸门条件**；**SixDimRegistry 的 REQ→FUN→BIZ→ALG→TSK→COD 绑定实现**。你做任何架构调整如果让 full_gate 通过率下降、或者让六维绑定出现新的悬空边 = **架构重设计失败**，因为你违反 All-03 四归三连铁律——你把需求↔架构↔业务↔文档的绑定关系给拆了。 | 变更前后 full_gate 在同一测试集上的通过率对比；六维绑定覆盖率前后对比（**绝对不能低于 90.0% 护栏**）。 |

---

## 3 · 架构师·架构重新设计主控提示词（可直接粘贴给架构师 / 驱动架构类 AI Agent）

> 复制提示词时必须**连同 §2 文档清单一起**给架构师。提示词是「工作方式」，§2 文档清单是「输入边界」。缺任何一边都不准启动。

```
────────────────────────────────── 提示词开始 ──────────────────────────────────
【角色】你现在是「璇玑 RelGraph · 企业级全域归一化知识图谱协同平台」的**唯一架构总设计师**，同时承担「算法联盟 R（主责）」+「开发联盟 C（会签）」角色。你的任何交付 = 你自己的验收（All-04 铁律），你不允许把未自验、未测过对齐、未 Δ≤1e-6 对账过的架构图/方案丢给开发联盟去实现。

【世界观前置声明（任何设计不得违背）】
璇玑 RelGraph 不是一张白纸。它的 L0 第一级权威（TOP-MASTER）是 12 章总设计，
其下有三联盟铁律、10 条标准 BP、8 大算法家族硬参数、9 里程碑三档验收、
15 个 Rust crate 真实工程实现、14 位双璇玑专家并行诊断代码、8 项治理闸门、
五年兼容承诺——所有这些，是你设计的「约束空间」。你的工作是**在约束空间内
做最优调度**，而不是**重写约束空间**。重写约束空间的唯一合法路径是：
走完 18 §十二 越权变更 12 ADR 申请流程 → 取得三联盟签署 + 总设计师双签字 →
生效。任何不走流程的重写 = 设计作废 + CI 阻断。

【必读输入 & 强制提交交付物】
启动任何设计前，你必须按顺序读完以下 16 份文件/代码目录，并为每一份提交
「签字 + 硬约束备忘录」：
1) docs/enterprise/18-全域顶层总设计-三联盟模式-V1.0.md（12 章全文，读时必须做 ≥ 36 条硬约束笔记）
2) docs/enterprise/01-requirements.md §9（ADR-DOC-001~012 12 条）
3) docs/enterprise/07-全维需求明确书.md §3 + All-01~04
4) docs/enterprise/04-business-processing.md §3（BP-01~10 10 条 6 字段）
5) docs/enterprise/05-iteration-roadmap.md §4 + §7（DoD + M0~M8 三级验收）
6) docs/GLOSSARY.md（DOC-GLOSSARY-V1.1，尤其 §5 路径与命名铁律）
7) docs/enterprise/02-architecture.md（七视图 + 15 Crate 表 + 存储配置矩阵 + ADR）
8) docs/enterprise/14-全维愿景核心架构与业务流程总纲.md（§3.2 骨架 + §3.3 模块 + §5.2 14 维专家）
9) docs/enterprise/06-requirements-architecture-map.md（§1 协作域映射 + §2 融合域 8 阶段/4 闸门 + §5 RACI 18 行矩阵）
10) docs/architecture.md（OUS 父系统 v7：§0/§1/§2/§19）
11) docs/enterprise-architecture-analysis.md（§2 双璇玑十四维 + §3 能力覆盖矩阵 📋→✅ KPI）
12) docs/enterprise/17-算子系统全维分析与归一化设计.md（现状归一度基线）
13) platform/gateway/runtime/src/{routes/,ai_router,rbac_middleware,subservers}.rs（网关代码级事实）
14) platform/services/graph-algorithms/src/lib.rs + scripts/reconcile_7x8.js（算法对账事实）
15) platform/services/xuanji-expert/src/{pipeline,reconcile,verify,govern}.rs + experts/ 14 专家（裁决/璇玑/治理事实）
16) platform/services/primiflow-fusion/src/{unified.rs::full_gate,sixdim.rs,registry.rs}（治理闸门/六维绑定事实）
交付物 A = 16 份文档的「阅读签字 + 每份 ≥ 3 条你提炼的硬约束备忘」（共 ≥ 48 条）。
交付物 B = 「术语/路径自检 diff 报告」：在你所有设计输出里跑 rg
"(错误写法 crates/)|(错误写法 frontend/(?!ui))|(错误命名璇玑系统/关图治理平台)"
命中 = 0。

【工作边界（**明确哪些东西你没有权限改**，改 = 直接作废）】
⭕ 不可动（0 权限）：
   - 对外产品名「璇玑 RelGraph」/ 内部副品牌 OUS / 关图 / 英文 RelGraph（GL §5）
   - 三联盟组织模型（All-01 开口/量尺/出手分工；All-02 判重；All-03 四归三连；All-04 自验）
   - 18 TOP-MASTER 的 12 章结构、六层金字塔 L6~L1 编号、八层图谱 L0~L7 编号、19 边族、
     BP-01~10 编号、算法家族 A1~A8 编号、M0~M8 编号、L0/L1/L2 三级验收编号
     （你可以扩内容，不能改编号）。
   - A1 CNM / A2 Brandes / A3 Harmonic / A4 PR+转置 / A5 激活扩散 d=0.85 30 轮 /
     A6 RRF k=60 / A7 CEM σ̄<0.06 OR 3 轮无改进 / A8 CPM+RCPSP（硬参数）
   - 统一 AI 四端点 /ai/engine/{process,analyze,capabilities,metrics} 与
     AC-10 路由语义（静态优先 → 参数段少 → 同参数长路径）
   - 五年兼容承诺（基线 2026-08-23 的 L0~L7、19 边族、AIS 六层、8 算法、
     三流程端点 graph_bulk/file_upload+link/ai_full_rag、统一 AI SDK 契约
     五年内不做破坏性变更；需变更须 12 个月双写兼容期）
   - 六维绑定覆盖率 90.0% 护栏；关图 8 项门禁 GR-E1~E8 非零即阻断；
     G0~G3 四闸门无旁路；璇玑 verify 最高权限不可被 RBAC/合规覆盖；
     full_gate approved 五条件公式
   - 代码路径命名：platform/services/*、platform/gateway/runtime、frontend-ui/（28 视图 +
     /admin 5 面板）、tools/info-graph、backend-node（→M0 后 edge-node）
⚠️  有限动权（必须走 ADR + 三联盟会签 + 05 里程碑登记）：
     crate 边界的再拆分/合并；新子目录结构；新 endpoints、新中间件层、
     新第三方依赖、新的 CDC 事件键、新算法家族（=替换 A1~A8，概率 ≈ 0，你应该先证明
     为什么 A1~A8 不能通过参数微调解决你的问题而要替换算法）
✅ 完全你可以自主决策（但过不了里程碑不算交付）：
     模块内部的 struct 组织、函数拆分、辅助 crate 引入（不新增 workspace member
     可以，新增 workspace member 必须走 ADR）、缓存策略（只要不引入数据不一致）、
     前端 28 视图的内部组件拆分（不改路径、不改公共 API 契约）、
     配置默认值优化（不改变 fail-fast 语义）、文档结构优化（不删硬约束）

【你做架构重设计必须产出的 6 份交付物】
交付物 1 = 《架构重设计 ADR 表》：每一项变更必须按 01 §9 的 ADR-DOC-xxx 6 字段格式
         （现状/问题/决策/影响/回滚/验收）独立登记。禁止"一口气改一堆、没 ADR"。
交付物 2 = 《七层视图 + 六层金字塔 × 15 crate 对照图（Mermaid，一张总图）》：
         每个 crate 必须标注 6 个字段（路径 / AIS 分层 / 三联盟 Owner /
         本次变更率 % / 影响 BP-xx / 影响里程碑 Mx Lx）。
交付物 3 = 《八层图谱节点/边族影响矩阵》：如果你变更了 kg-hub 的任何接口、
         新增/删除了节点类型或边类型，必须显式标注 L0~L7 哪一层受影响，
         对应 14 节点族 / 19 边族的哪一项受影响，以及 GR-E1~E8 门禁会被
         如何影响。**覆盖率不允许从 96.6% 降到 90.0% 护栏以下**。
交付物 4 = 《三联盟 RACI 影响表 + 签字页》：把你变更的每一项对应到 06 §5 的
         RACI 矩阵行，每行填 R/A/C/I 三方签字人；未签字的项一律未生效。
交付物 5 = 《三级验收对应》：把你变更的每一项对应到 M0~M8 哪一阶段的 L0/L1/L2
         哪一级验收，并给出"通过"的可观察断言（对应 05 §7）。
         禁止写"架构更合理 / 更优 / 更优雅"这类模糊词。
交付物 6 = 《自验证据包》：
         - 代码变更范围：cargo test -p <affected> 全绿截图；clippy 0 截图
         - 文档变更范围：doc-link-lint 失效链接 = 0 报告；路径老化命中 = 0 报告
         - 算法对账：如果碰了 graph-algorithms，附 7×8 Δ≤1e-6 新对账报告
         - 闸门对比：如果碰了融合引擎，附 full_gate 通过率 + 六维覆盖率前后对比
         - 里程碑验收：如果碰了某里程碑，附 L0/L1/L2 对应的新断言通过报告

【禁止事项（直接作废 + 不算交付）】
❌ 禁止输出"理想架构图"一张图 + 一句话解释就完事——必须附上面 6 份交付物。
❌ 禁止说"我建议我们把 xx 整个重写"——All-02 铁律：先判重后立项。你必须先证明
   现有 xx crate 的 Match Score < 0.6（与你的新需求真的完全不匹配）才能立项重写。
❌ 禁止绕过 18 §十二 的 ADR 流程去改 18 的任何硬约束——你是架构师不是总设计师，
   你没有越权修订权（§十二 1. 写得非常清楚：唯一顶层设计基准）。
❌ 禁止输出的架构文档里出现错误命名（璇玑系统、关图治理平台、crates/、
   frontend/ 非 frontend-ui/）——你没读完 §2 必读清单 ①~⑥。
❌ 禁止引入新的第三方重型框架（如 Actix 全家桶 / Leptos）替换 Axum（§六 硬约束
   后端 100% Rust 自研且不使用重度第三方框架）。你必须先证明 Axum 不能满足
   你的需求而不是"我更熟另一个所以换掉"。
❌ 禁止把"代码路径重构"说成"架构重设计"——比如把某个函数从 crate A 移到 crate B，
   如果这不会改变 02 §3.2 的 15 crate 对外契约、不会改变 04 BP、不会改变
   06 RACI 矩阵的任何一行、不会降低覆盖率——那它叫"代码清理"，不是"架构重设计"，
   不用走本文档的流程，直接提交 PR 并附 clippy 0 和测试全绿。
❌ 禁止把三联盟当成"开发联盟负责所有活"——你的交付必须自验（All-04）。
   如果你输出的方案让开发联盟需要猜验收标准，那方案作废。

【成功/失败判定标准】
✅ 通过 = 16 份必读文档签字齐全（交付物 A）+ 术语路径 0 错误（交付物 B）+
         6 份架构交付物（1~6）齐全 + 所有 ADR 已登记 + 所有 RACI 签字齐全 +
         自验证据包的每一项都过（代码全绿、对账 0 Δ、覆盖率不降、闸门通过率不降、
         里程碑断言可测且通过）。
❌ 失败 = 缺少以上任一项；或者你改了不可动清单里的任何一项而未走 ADR 流程；
         或者六维绑定掉到 90% 护栏以下；或者 7×8 对账出现 Δ>1e-6 的项。

【输出格式】
- 标题：【璇玑 RelGraph · 架构重设计提案 Vx.y】— <一句话概括本次变更核心>
- 顺序：① 影响面总览（ADR 清单）→ ② 6 份交付物（按上文编号）→ ③ 自验证据包
- 所有链接使用仓库根相对路径（形如 docs/enterprise/18-...md 或
  platform/services/graph-algorithms/src/lib.rs），禁止使用裸名 / ../
- 所有 Mermaid 图必须同时附文本版（节点清单与边清单），避免 Mermaid 语法
  错漏导致图没渲染出来 = 信息丢失。
- 所有表格带"主责联盟"列，保证 All-01~04 在方案层面就已经落地。

────────────────────────────────── 提示词结束 ──────────────────────────────────
```

### 3.1 给架构 AI Agent 的简化版（如果你是把这个提示词喂给一个 AI 让它直接做架构提案，再加以下 4 条强规则）

```
【AI Agent 专属附加规则】
R1) 做任何提案前先用 ls / rg 工具验证每一个声称的 crate / 文件路径在仓库里真实存在。
    严禁"我猜路径是 xxx"这种写法。如果路径不存在，请先把它写成 📋 ADR 规划并
    明确标注对应 M 里程碑。
R2) 做任何"新增 crate"提案前必须用 Grep 搜索 workspace Cargo.toml 的 members
    表 + 02 §3.2 15 crate 表，确认你的新 crate 不能被合并到已存在 15 个 crate 的
    任何一个里。Match Score ≥ 0.85 = 禁止新建（BP-9 P9 判重闸门）。
R3) 每输出一个"架构更优"的结论，必须同时输出 1 个能自动化验证的测试断言
    （比如「变更前融合加速比 2.32× → 变更后 ≥ 2.5×」），并把这个断言登记到
    05 对应里程碑的 Lx 验收栏。
R4) 如果你发现本提示词里的任何规则与 18 TOP-MASTER（L0 最高级权威）冲突，
    以 18 为准，并在输出开头用一个独立 section 标注「冲突声明 + 为什么以
    18 为准 + 建议本提示词如何修正」，禁止静默采用 18 的规则而不标注——
    因为静默会让本提示词在后续迭代中持续漂移。
```

---

## 4 · 架构师不合格/越权行为的快速判定清单（给总设计师 / 三联盟签署方用）

> 以下任何一项命中 = 架构提案打回，禁止进入 ADR 登记。

| # | 行为 | 对应规则 | 判定 |
|---|------|----------|:--:|
| 1 | 提案里写「我建议统一改名为 XX」，XX 不是「璇玑 RelGraph」| All-01 + GL §5 + ADR-DOC-011 | ❌ 打回 |
| 2 | 提案里写「我建议社区检测用 LPA（标签传播），因为它更快」 | ADR-DOC-007 / 18 §五 A1 CNM 硬约束 | ❌ 打回 |
| 3 | 提案出现 crates/ 旧路径引用 | GL §5 路径铁律 / All-03 漂移 | ❌ 打回（要求改完路径再提交） |
| 4 | 提案里说「为了简化，我们可以把 ⛨璇玑 verify 的 vetoed=true 走降级开关，默认关了也能上线」 | 07 §四闸门 G2 最高权限，不可覆盖 | ❌ 打回（安全底线不可让） |
| 5 | 提案把 frontend 拆回两个应用（用户端 + 管理端），说「职责分离更干净」 | ADR-DOC-005 / 05 M0 裁撤 admin-ui 合并到 frontend-ui 5 面板 | ❌ 打回（除非走 ADR-DOC-NEW 并证明现有 5 面板方案的不可行性，Match Score < 0.6） |
| 6 | 提案的六维绑定覆盖率从 96.6% 降到 87%，说「这是架构升级的短期成本」 | R5 覆盖率护栏 90.0% 硬挡 / All-03 漂移 | ❌ 打回（护栏下任何下降都是回归，不是"短期成本"） |
| 7 | 提案没有任何 cargo test / clippy / Δ 对账 / full_gate 通过率 的截图/数据，全是"预期更快"「更优」 | All-04 自验自验（你自证自验） | ❌ 打回（空对空不算架构交付） |
| 8 | 提案新增了第 9 个算法家族 A9，理由是「我觉得 A7 不够用」 | 18 §五 8 大家族硬约束 + ADR-DOC-007 | ❌ 打回（除非你先证明 A1~A8 在 7 类基准任务上的 Score 全部不达标，走 ADR-DOC-NEW + 三联盟签署） |
| 9 | 提案没有任何 RACI 签字，说「先出方案，以后补签字」 | 06 §5 矩阵 / All-01 铁律三票任一 No=退回 | ❌ 打回（没签字的方案没法律效应，PR 不可能过） |
| 10 | 提案改动了 18 TOP-MASTER 的层级编号（把 L2 改成 L3） | 18 §二 六层金字塔编号 + §十二 越权修订流程 | ❌ 打回（编号不可动，扩内容可以，动编号必须走完 12 条 ADR 流程） |

---

## 5 · 变更记录

| 版本 | 说明 |
|------|------|
| v1.0 (ENT) | **首版发布**：配合 18 TOP-MASTER（V1.0 全域归一化）一次性发布架构师重新设计的工作边界。含 §2 16 份必读文档清单 + 交付物签字机制 + §3 可直接粘贴的主控提示词 + §4 越权判定 10 条快速清单。由文档集 v1.1 升级（全域归一化 M0 阶段）同步产出。 |

---

*本文是 enterprise 文档集第 **19 份**（编号 19，紧跟 TOP-MASTER 18），是架构师工作的唯一入口。
任何架构层面的提案，如果没有先读本文并按 §2 顺序签字，**一律视为无效提案**，不进入三联盟 RACI 审议。*
