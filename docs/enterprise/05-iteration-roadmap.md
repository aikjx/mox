# 璇玑 RelGraph · 迭代与优化路线图（9 里程碑 M0~M8 · 三级验收版）

> **文档类型**：持续改进路线图 / Backlog / 企业级 DoD / KPI / 风险登记 · **9 里程碑 M0~M8 统一口径**
> **文档版本**：v1.2 (ENT) · 最后更新 2026-08-23
> **权威链**：🟢 L0 → [`18-全域顶层总设计-三联盟模式-V1.0.md`](18-全域顶层总设计-三联盟模式-V1.0.md)（TOP-MASTER §八：9 里程碑三联盟协同推进与三级验收）。
> **主责联盟**：三联盟联合签署（产品=目标定义 · 算法=里程碑校验 · 开发=实施与交付）
> **配套**：`00-INDEX`（治理）、`01-requirements`、`02-architecture`、`03-design`、`04-business-processing`
>
> 本文把「分析 → 整理 → 优化 → 迭代」固化为**可执行的工程节奏**，并以 **M0~M8 九阶段 + L0/L1/L2 三级验收**对齐 18 TOP-MASTER §八，使文档集持续向「最高标准企业级架构」收敛。

---

## 1. 持续改进机制（Process）

| 节奏 | 活动 | 产出 |
|------|------|------|
| **每次 PR** | 行为/接口/权限变更须同步 `01/03/04` + 变更记录 | 文档不漂移 |
| **双周迭代** | 领取 backlog 项 → 实现 → 测试 → bump 次版本 | 增量可验证 |
| **季度架构评审 QAR** | 核对 `02-architecture` 与代码事实偏差（沿用 `enterprise-architecture-analysis` 校验法） | 架构不腐化 |
| **发布 ENT** | 文档集全量评审（RACI）通过 → 主版本 bump | 企业级发布 |

**原则**：发现缺陷 → 登记 `BR-/GAP-` → 进 backlog → 修复 → 回填追踪矩阵 + 变更记录（GAP 闭环）。

---

## 2. 当前成熟度评估

| 维度 | 成熟度 | 说明 |
|------|:--:|------|
| 功能完整性 | 🟢 高 | 成员/任务/权限/通信四大子系统落地 |
| 安全闭合 | 🟢 高 | GAP-2 跨租户提权已闭环；鉴权留痕 |
| 流程闭环 | 🟢 高 | 8 BP + 2 FSM + 21 BR，6 GAP 全固化 |
| 测试回归 | 🟢 高 | mox-system 实测 **82 测试全绿**（bin 55 + lib/main/集成 27）；覆盖越权/事件/继承/持久化 |
| 持久化 | 🟢 高 | 多后端写透 + 启动重放（I-01/I-02 已落地，重启不丢、幂等）；SQLite 默认 + PostgreSQL/MySQL 可切（I-16），生产 fail-fast |
| 可观测性 | 🟢 高 | Prometheus 指标 + 限流 + CORS 加固（I-04 已落地） |
| 配额边界 | 🟢 高 | 成员/任务/分派/子任务/依赖深度上限可配可拒（I-03 已落地） |
| 多租户策略 | 🟢 高 | 查询隔离✅；策略分层已落地（I-06 已交付，2026-08-17） |
| 灾备 | 🟡 中 | SQLite 单文件可冷备，PG/MySQL 可复用原生主从与 PITR；WAL 快照/混沌未做（I-12 待办） |

---

## 3. 优先级待办（Backlog）

### P0 — 安全/正确性（必须先行）

| ID | 项 | 关联 | 验收标准 | 状态 |
|----|----|------|----------|------|
| I-01 | **持久化 Store**：SQLite 实现 `Store` trait，替换内存态 | NFR-03 | 重启后璇玑/成员/任务/事件不丢失；接口不变 | ✅ 已落地 |
| I-02 | **审计链 WAL 持久化 + 重放**：`AuditChain` 落盘，支持事件重放 | BR-19/BR-20 | 进程重启后审计可查；重放幂等 | ✅ 已落地 |
| I-03 | **配额约束**：成员数/任务数/依赖深度上限可配置 | NFR-09 | 超限返回 4xx；默认上限生效 | ✅ 已落地 |
| I-16 | **多后端持久化 + 生产 fail-fast**：`trait Repository` 抽象 SQLite/PostgreSQL/MySQL，12-Factor 切换；`MOX_STRICT_PERSIST` 连库/建表失败即中止启动 | NFR-03 / ADR-07 | 三后端零代码切换、默认 SQLite；严格模式下连库失败启动中止而非静默回退内存 | ✅ 已落地 |

### P1 — 企业级能力

| ID | 项 | 关联 | 验收标准 | 状态 |
|----|----|------|----------|------|
| I-04 | **可观测性**：Prometheus 指标 + 限流 + CORS 加固 | NFR-08 | `/metrics` 暴露；限流 429；CORS 默认拒绝 | ✅ 已落地 |
| I-05 | **双验收联动**：任务 Done ∧ 融合验证通过 才允许 `/publish` | BR-16 | 任一项不满足→拒绝上架 | ✅ 已交付（2026-08-17） |
| I-06 | **租户维度策略分层**：`GovernContext.tenant` → 十四维策略按租户覆盖 | 架构 §5.2 | 不同租户策略隔离生效 | ✅ 已交付（2026-08-17） |
| I-07 | **产物来源追溯**：上架产物带璇玑/任务 ID + 优化前后指标 | BR-17 | 元数据不可缺 | ✅ 已交付（2026-08-17） |
| I-15 | **全维度开发完成（里程碑）**：双璇玑十四维 + 四闸门 + 流程图归一化 + 全维自动化处理，全部经测试验证 | `07`/`08`/`09` | 双璇玑契约测试 14 维齐全、越权被拦、端到端跑通、workspace 全绿 | ✅ 已达成（见 `09`） |

### P2 — 增强与体验

| ID | 项 | 关联 | 验收标准 |
|----|----|------|----------|
| I-08 | **开发七维常驻**：存量代码 AST 扫描批量提取 `CodeIR`，开发璇玑可独立运行 | 架构分析 §4.2 | 无 LLM 也能跑开发七维 |
| I-09 | **ABAC 属性权限**：在 RBAC 之上叠加属性策略（时间/地点/风险分） | 安全视图 | 属性策略可配可审 |
| I-10 | **敏感语义识别**：`SENSITIVE_PATTERNS` 叠加上下文语义 + 租户自定义词表 | 架构分析 §4.2 | 误报率下降、可配置 |
| I-11 | **璇玑校验可解释报告**：`AlgoVerification` 产出可读「为何通过/拦截」 | 架构分析 §4.2 | 报告可导出合规 |

### P3 — 演进

| ID | 项 | 说明 |
|----|----|------|
| I-12 | 灾备：快照 + 混沌工程（对齐 `docs/architecture.md` §16） |
| I-13 | 多模态/记忆/Eval/FinOps 从设计态落地（架构分析 §3 矩阵 📋 项） |
| I-14 | 分片反应器：按璇玑分片，支撑水平扩展 |

---

## 4. 企业级「完成定义」（Definition of Done）

一份文档/一项功能达到企业级，须同时满足：

- [ ] **需求可验证**：每条 FR/NFR 映射代码或测试，GAP 有双向断言（`01` §追踪矩阵）。
- [ ] **架构多视图一致**：业务/信息/应用/技术/安全/集成/部署视图无矛盾（`02`）。
- [ ] **设计可追溯**：模块→文件→接口契约明确（`03`）。
- [ ] **流程可闭环**：生命周期 FSM + BR 目录覆盖（`04`）。
- [x] **安全可审计**：RBAC/作用域/审计链/威胁模型齐备且经测试（2026-08-18：RBAC 六角色矩阵兼容/严格双模式 + 双向签名审计经 E2E 实测 5/5，见 `12`）。
- [x] **质量门禁**：`cargo clippy` 零告警、`cargo test` 全绿（644/0）、集成测试覆盖越权路径（`test_rbac_viewer_denied_write` 403 实测）。
- [ ] **可观测**：关键路径指标可采集（NFR-08 落地后）。
- [ ] **文档同步**：变更在合并前同步，变更记录留痕。
- [ ] **全维双收口（铁律，见 `07`）**：任何功能须先过 `07` §3 双闸门——①业务流程图归一化（唯一 `FlowGraph` + 六维绑定 + 过 G0）；②开发璇玑全维分析验证（`run_experts` 14 专家无 `veto` + 璇玑 `vetoed=false` + 治理 `approved=true` + `primiflow_fusion::full_gate` 全绿）。**未经双收口的功能一律退回，禁止无验证扩展、禁止模糊需求、禁止给闸门加旁路**。

---

## 5. 关键绩效指标（KPI）

| KPI | 基线 | 目标 |
|-----|------|------|
| 跨租户提权拦截率 | 100%（修复后） | 维持 100% |
| 测试通过数 | 642（2026-08-17 实测 `cargo test --workspace`） | 随功能增长保持全绿 |
| 融合加速比 | 2.32× | ≥2.32× 且不退化 |
| clippy 告警 | 0 | 0 |
| 持久化覆盖率 | 0%（内存） | 100%（I-01 后）✅ 已达 |
| 可观测指标覆盖 | 0 | 核心路径 100%（I-04 后）✅ 已达 |
| clippy 告警 | 0 | 0 ✅ |
| 文档漂移 | 手工同步 | PR 卡点强制同步 |

---

## 6. 风险登记（Risk Register）

| 风险 | 影响 | 可能 | 缓解 |
|------|------|:--:|:--:|------|
| 内存态数据丢失 | 高 | 中 | I-01 持久化；WAL(I-02) |
| 审计噪声淹没真实越权 | 中 | 低 | 两段式鉴权（已实现） |
| 配额缺失致资源耗尽 | 中 | 中 | I-03 配额 |
| 租户策略串扰 | 高 | 低 | I-06 策略分层 |
| 文档与代码漂移 | 中 | 中 | PR 同步卡点 + QAR |
| 权限探测（实时通道反馈） | 中 | 低 | AuthzDenied 不回推（已实现） |

---

## 7. 迭代路线图（9 里程碑 M0~M8 · 三联盟协同 + 三级验收 L0/L1/L2 · 对齐 18 TOP-MASTER §八）

> **三级验收定义（强制）**：
> - **L0 单元 & 静态**：相关 crate `cargo test` 全绿 + `cargo clippy` 零告警；前端 28 视图构建零错；文档不漂移。
> - **L1 集成 & E2E**：跨 crate / 跨 BP 集成测试全过；BP-1~10 至少各一条端到端冒烟；RBAC 11 探针 + 5 E2E 通过。
> - **L2 SLO & 可验证**：关键指标达标（融合加速比≥2.32×、关图覆盖率≥96.6%、判重复用率↑、可用性≥99.9%）；有外部审计或客户验收记录。

| 里程碑 | 名称（对齐 18 §八） | 主责联盟 | 核心交付物（必须可验证） | L0 验收 | L1 验收 | L2 验收 | 现状 |
|--------|---------------------|:--:|--------------------------|:--:|:--:|:--:|:--:|
| **M0** | **全域归一化**（术语/路径/命名/文档/目录/算法/架构/治理 八归） | 三联盟联合（开发 R，产品/算法 C） | frontend-ui 单应用（+ /admin 5 面板）；platform/services 15 crate；18 TOP-MASTER 发布；01 §9 ADR-DOC-001~012 注册；GLOSSARY 7 新术语；双 README 统一；路径 `crates/`→`platform/domains/` 零残留；前端 admin-ui 裁撤 | workspace 649+ 全绿；clippy 0；前端 build 0 错 | 19 份权威链全部回指 18；路径搜索 `crates/` 命中=0 | BP-10 文档同步治理 CI 通过（PR 卡点）；P9 判重闸门在 3 条真实需求上复用率 60%+ | 🟢 **进行中（本轮完成）** |
| **M1** | **业务闭环增强**（10 BP 全部 L2 达标） | 产品联盟 R + 开发联盟 | BP-1~10 全部有独立 E2E；BR-16/17 全量从「部分」→「完成」；双验收联动发布端全链路闭环；ABAC 属性权限、敏感语义识别、璇玑可解释报告 3 项交付 | L0 + 新增 20 条 BP 用例；I-08/09/10/11 对应测试 | 10 BP × 至少 1 冒烟全过；RBAC ABAC 双模式兼容 11 探针；双验收流程「组织未 Done → 拒绝」实锤 | 真实客户 1 家试点流程 0 阻断；DoD 门禁误拦截率 < 1%；判重复用率提升 20pp | 🟡 下一阶段 |
| **M2** | **算法核落地**（八大算法家族单测 + 对照基准 + 线上路由） | 算法联盟 R + 开发联盟 | A1 CNM / A2 Brandes / A3 Harmonic / A4 PageRank / A5 激活扩散 / A6 RRF / A7 CEM / A8 CPM·RCPSP 8 × 至少 5 组对照基准；Node ↔ Rust 算法 Δ≤1e-6 7×8 对账矩阵；激活扩散接入统一 AI 路由意图识别 | graph-algorithms crate 单测全绿；基准 Δ 全部达标；无 LPA/近似介数残留实现 | 意图识别 A/B 测试：Top-1 准确率 ≥ 85%；RRF 融合 5 路召回 MRR 提升 | 真实线上 AI 请求 σ̄ < 0.06（CEM 收敛）；算法家族单测覆盖率 ≥ 90% | 🟡 下一阶段 |
| **M3** | **统一 AI 编排**（Rust Gateway 四端点 + AC-10 路由语义） | 开发联盟（Rust Gateway 组）R + 算法联盟（A5/A7） | `/ai/engine/{process,analyze,capabilities,metrics}` 四端点全上线；AC-10 静态路由优先语义（参数段少者优先、同参数数时保留长路径）单元测试；Caomei 旧 AI 路由全部下线，全量走 Gateway；统一协议（请求 intent → 响应 capability.route + metrics.latency_ms） | Gateway 单测全绿；路由语义测试集 ≥ 30 条（静态/参数/混合）全部 pass；`/metrics` 报告 | 28 视图中 5 个 AI 视图（Chat/Caomei/AlgoLab/InfiniteOptimizer/LLM）全部改为 Gateway 调用；E2E 5 条全过 | 路由成功率 ≥ 99.5%；P95 AI 查询延迟 ≤ 1000ms；降级率 < 2% | 🟡 M2 后启动 |
| **M4** | **分布式存储底座**（NebulaGraph/Neo4j 图谱 + MinIO 对象 + Postgres+Citus 元数据） | 开发联盟 · 存储组 R + 算法联盟（图谱读写） | 图谱后端接口 `GraphStore trait` 双实现（NebulaGraph / Neo4j）；MinIO 对象存储替换本地文件；Postgres + Citus 多租户分片；双写灰度 T1 方案 | 三后端单测/集成全绿；迁移脚本幂等；T1 双写一致性（校验 1 万条 Δ=0） | 10k 节点 / 20k 边读写 < 100ms；宕机切换 RPO=0 / RTO<60s | 200 次 EC kill-2 混沌演练 CRC 100%；存储成本对比对象存储下降 ≥ 40% | 🟡 M3 后启动 |
| **M5** | **可观测 + HA 高可用**（Prometheus + Grafana + Tracing + 混沌） | 开发联盟 · 运维组 R | 指标（鉴权拒绝率/状态迁移/延迟 p50/p95/p99）、Tracing（OpenTelemetry 埋点 8 段流水线）、告警（SLO 0.5% 偏差即报）；多副本 + 优雅流量切换 + 旧服务关停 | Prometheus /metrics ≥ 60 指标；告警规则 ≥ 20 条；Tracing 覆盖全流水线 | 注入 100 故障：延迟抖动、依赖超时、内存不足，SLO 不退化 | 99.95% 可用性达标（月度宕机 < 22min）；RPO=0；RTO<60s | 🟡 M4 后启动 |
| **M6** | **多云与多租户强化**（K8s 部署 / 多租户策略 / 灾备 PITR） | 开发联盟 · 云原生 R + 产品联盟 | K8s Helm Chart；策略分层（政务/金融/普通商业）差异化；WAL 快照 + PITR 回滚演练 | Helm 安装一次 OK；3 种租户策略 E2E 全绿；PITR 任意时间点回滚误差 < 1s | 真实多云（阿里云 + 腾讯云）各部署一套可用；跨云流量切换 ≤ 5min | 租户间隔离 100% 通过「跨界注入」攻防演练；灾备 SLA 对外承诺签署 | 🟡 M5 后启动 |
| **M7** | **生态与市场化**（模板市场 / Skill SDK / 外部联盟接入） | 产品联盟 R + 开发联盟 | 模板市场 ≥ 100 套；Skill SDK 文档 + 示例 ≥ 20；外部联盟（客户/ISV）接入 3 家以上；算子市场对外发布 | 市场单测 + 集成全绿；Skill SDK Hello World 5 分钟跑通 | 外部 ISV 发布 ≥ 5 套模板并通过 G0~G3 全闸门 | 转化率（免费试用→付费）≥ 5%；复购率 ≥ 30% | 🟡 M6 后启动 |
| **M8** | **自治与持续优化**（CEM σ̄<0.06 自动收敛 · 自修复闭环） | 算法联盟 R（A7 CEM） + 开发联盟 | CEM 持续调优 AI 引擎（停止条件 σ̄<0.06 或 3 轮无改善）；关图 sync 漂移自动修复；AI 自测 / 自回归 / 自愈触发 | CEM 7 类任务（数学/逻辑/知识/代码/中文/时效/指令）上的加权分 0.55×质量 + 0.20×速度 + 0.10×token 效率 + 0.15×稳定性 提升 ≥ 5% | 7 大类 × 3 轮 = 21 子任务，自动收敛率 ≥ 85%；无人工干预闭环成功 ≥ 3 次/周 | 每月架构/算法/文档的漂移归零（偏离 GR-E1~E8 全部自动修复）；人工干预下降 ≥ 60% | 🔵 远期（2026 Q4+） |

> 每完成一个里程碑，在「变更记录」登记并 bump 主/次版本；**必须 L0+L1+L2 全过才能宣布里程碑完成**，禁止降级宣布。
> **与既有迭代对应**：M0 = 原「迭代 1+2」的归一化收口 + 文档化；M1 = 原「迭代 3」增强；M4~M6 = 原「迭代 4+」演进 + 存储/HA/多云补齐。

---

*本文是文档集的「活尾部」：随系统演进持续更新。与 `00-INDEX` 治理机制共同保证「最高标准企业级架构」可持续达成。*

---

## 8. 变更记录（Changelog）

> 遵循「每次 PR 同步变更记录」治理：本表的实现项均已在 `crates/mox-system` 落地并经 `cargo test` 与 HTTP 端到端冒烟验证。

| 日期 | 版本 | 变更 | 关联 | 验证 |
|------|------|------|------|------|
| 2026-08-16 | v1.1 (ENT) | **I-01/I-02 持久化**：`store.rs` 增加 SQLite 写透 + 启动重放；令牌 SHA-256 仅存哈希；审计链落盘且计数与 `/metrics` 共享原子量 | NFR-03/BR-19/BR-20 | `persistence_survives_restart`、`store_persists_and_hashes_tokens` 通过；重启幂等、旧令牌仍有效 |
| 2026-08-16 | v1.1 (ENT) | **I-03 配额**：`config.rs` 12-Factor 配额；`services.rs` 在 invite/create/assign/subtask/dependency 处强制上限 | NFR-09 | `quota_enforcement` + `nfr09_*` 系列通过；超限返回 4xx |
| 2026-08-16 | v1.1 (ENT) | **I-04 可观测+加固**：`metrics.rs` Prometheus 文本指标；`ratelimit.rs` 固定窗口限流；`server.rs` CORS 默认拒绝+允许名单、`/metrics` 端点、`/health` 上报真实持久化态 | NFR-08 | HTTP 冒烟：限流 200/200/429、CORS 拒绝非白名单、指标可采集 |
| 2026-08-16 | v1.1 (ENT) | **修复**：内存模式（`MOX_PERSIST=0`）曾忽略 `MOX_BIND/RATE_LIMIT/CORS`，改为统一走 `with_config(config)`，仅由 `persist` 决定是否落盘 | 配置一致性 | 内存态现正确绑定到指定端口并生效限流/CORS |
| 2026-08-16 | v1.1 (ENT) | **测试**：`tests/integration.rs` 新增 3 项（持久化/令牌哈希/配额）；全量 19 测试通过 | 质量门禁 | `cargo test -p mox-system` 全绿 |
| 2026-08-17 | v1.2 (ENT) | **I-16 多后端持久化**：`repo/` 抽象 `trait Repository`，`schema.rs` 按 `sea-query` 方言生成 DDL/upsert，落地 `sqlite.rs`/`postgres.rs`/`mysql.rs`；`MOX_BACKEND`+`MOX_DB_URL` 12-Factor 切换，默认 SQLite | NFR-03 / ADR-07 | `cargo check -p mox-system` 通过；三后端方言 upsert 分别生成 `INSERT OR REPLACE`/`ON CONFLICT`/`ON DUPLICATE KEY` |
| 2026-08-17 | v1.2 (ENT) | **I-16 生产 fail-fast**：新增 `MOX_STRICT_PERSIST`；`build()` 改为返回 `Result` 使错误经 `with_config` 上浮至 `main`（规整致命信息 + 非零退出码，非 panic backtrace）；覆盖「连接失败」与「建表失败」两条路径 | NFR-03 / 可运维性 | 严格模式下连库失败启动中止；默认关闭保持演示/测试兼容 |
| 2026-08-17 | v1.2 (ENT) | **可观测性**：启动日志由硬编码「SQLite」改为如实回显 `backend` 与严格模式状态，避免运维误判实际生效后端 | NFR-08 | 日志输出 `持久化模式: 开启 (后端=Postgres, 严格模式=开(...))` |
| 2026-08-17 | v1.2 (ENT) | **文档归一化**：持久化配置矩阵收口为 `02` §7.4 唯一权威基准，根 `README` 改为指针；同步修正 `00`§3 自查清单、`01`§3.2/NFR-03/假设 A-1（新增约束 C-3）、`02`§2.3/§7.3/ADR-03→ADR-07、`03` 设计取舍、`06` 映射表中「内存实现/仅 SQLite 可替」的陈旧陈述 | 文档治理 | 全仓 `MOX_*` 配置仅一处定义，无重复/矛盾源 |
