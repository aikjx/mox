-- =============================================================================
-- ⚠ 已废弃 / 已合并 —— 不要单独安装本文件
-- =============================================================================
-- 本文件原是“P17 模块注册 + 知识图谱（mox_sys_* 10 表）”的独立包。
-- 为消除“重复设计”，这 10 张表的定义已并入唯一权威母版：
--     mox_sys/mox_sys-universal-template.sql   （单一归一化 DDL，含全部 56 张表）
--
-- 一键安装只需执行该单一文件，详见同目录 install.sql / install.ps1。
-- 本文件仅保留作历史指针，不再包含任何可执行 DDL。
-- =============================================================================
