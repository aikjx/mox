[package]
name = "mox-data-catalog-svc"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "用璇玑架构把全部业务建模为流程图 + 六维关系网，并在使用中不断优化"

[dependencies]
mox-platform-foundation = { workspace = true }
mox-ai-flow-sdk = { workspace = true }
# 专家领域协议层（foundation 层，Gate 跨域检测跳过）
# DIP：本 crate 只依赖 trait + 数据投影，不依赖 concrete ExpertServiceImpl
mox-ai-expert-proto = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tokio = { workspace = true }
async-trait = { workspace = true }

[dev-dependencies]
# DIP 验收测试需要 concrete 工厂 default_consultant() 构建 trait object
mox-ai-expert-svc = { workspace = true }
