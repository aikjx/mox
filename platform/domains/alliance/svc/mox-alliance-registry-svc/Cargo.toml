[package]
name = "mox-alliance-registry-svc"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "专家注册中心服务（v3 拆分）"

[[bin]]
name = "mox-alliance-registry"
path = "src/bin/main.rs"

[dependencies]
mox-api-crypto = { workspace = true }
# 共享协议
mox-alliance-common-proto = { workspace = true }
mox-alliance-registry-proto = { workspace = true }
# 分级心跳聚合核心（node→rack→cell 10:1:1，纯计算零 IO）
mox-alliance-registry-core = { workspace = true }

# Web 框架
axum = "0.7"
tokio = { version = "1.0", features = ["full"] }
tower-http = { version = "0.5", features = ["cors", "trace"] }

# 工具
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
parking_lot = "0.12"
rusqlite = { version = "0.31", features = ["bundled"] }

[dev-dependencies]
# 测试用依赖（HTTP 端到端：axum Router + oneshot）
tower = { workspace = true, features = ["util"] }
http-body-util = { workspace = true }
mox-alliance-registry-core = { workspace = true }
chrono = { workspace = true }

[lints]
workspace = true
