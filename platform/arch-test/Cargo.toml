[package]
name = "mox-arch-test"
version = "1.0.0"
edition = "2021"
description = "MOX Architecture Tests: enforce cross-domain dependency rules and layering constraints"

[dependencies]
toml = "0.8"
serde = { workspace = true }
serde_json = { workspace = true }
walkdir = "2"

[dev-dependencies]
