[package]
name = "operator-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description.workspace = true

[features]
# wasmer_fuel_backport：如果 wasmer 后续暴露 Store::set_fuel / fuel_remaining，启用本 feature 可接入真实 fuel metering。
# 当前保持 off（O3 memory_pages 硬上限 100% 工作，fuel 部分 best-effort）。
wasmer_fuel_backport = []

[dependencies]
mox-common-meta = { path = "../mox-common-meta" }
operator-core = { path = "../operator-core" }
wasmer = { workspace = true }
wasmer-compiler-cranelift = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
